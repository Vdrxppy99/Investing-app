.claude/
portfolio-backup-*.json
.DS_Store

# push server local secrets + wrangler temp
worker/.dev.vars
worker/.wrangler/
.wrangler/

# generated repo dumps — never commit (2.6MB, and it mirrors seed data)
repomix-output.xml

# xcode derivedata — build intermediates, object files, dylibs, module caches
native/build/
