.claude/
portfolio-backup-*.json
.DS_Store

# push server local secrets + wrangler temp
worker/.dev.vars
worker/.wrangler/
.wrangler/

# generated repo dumps — never commit (2.6MB, and it mirrors seed data)
repomix-output.xml

# local agent scratch
.claude/worktrees/
_to_delete/

# Phase 0 test tooling (node + Playwright) — dev-only, never shipped to production
node_modules/
test-results/
playwright-report/
playwright/.cache/

# Xcode build products — regenerated every build, was bloating every diff
native/build/
