# Changelog

History condensed to version notes on 2026-07-10 (repository history was rewritten to remove personal data).

- **2026-07-02** · Add files via upload
- **2026-07-04** · Add files via upload
- **2026-07-04** · Add files via upload
- **2026-07-04** · Make Markets and Insights tap-through; remove rebalance, fees, trade stats
- **2026-07-08** · Fix profit chart glitch: live price ticks used the old deposits-based profit formula, spiking the last chart point above the lots-based line (W/V shapes)
- **2026-07-08** · **v3.1**: fix W/V chart glitch at the source — never overwrite fresh quotes with 15-min-delayed stooq prices, reject implausible ticks (>25%), poll 10s instead of 3s, visible ver
- **2026-07-08** · **v4.0**: full visual redesign — Inter typography, cobalt design system, dark/light themes, validated chart palette, micro-animations
- **2026-07-09** · **v4.1**: hero balance card; right chart for each data type — stacked bars for asset location/exchanges, ranked bars for sectors, center-labeled allocation donut
- **2026-07-09** · **v5.0**: brand identity — emerald-on-black theme, real app icon + PWA manifest, greeting header, Today's-mover card
- **2026-07-09** · **v6.0**: paid-app interactions — chart scrubbing (drag to see any date's value in the header), axis-less hero chart with glow + min/max labels, gradient stroke, film-grain depth
- **2026-07-09** · **v7.0**: guidance features that paid apps charge for — savings Goal tracker (progress ring + projected finish date from real return + contribution pace) and Portfolio Health Sco
- **2026-07-09** · **v7.1**: UI polish + universal clickability — SVG line-icon header (theme/currency/gear/refresh), tappable stat chips and P/E/Risk/Health cards that open explainer sheets, progr
- **2026-07-09** · **v7.2**: stock search (Markets), pull-to-refresh, service worker (offline + instant load, network-first HTML so updates always land, never caches live price data)
- **2026-07-09** · **v7.3**: Performance section (deposit-adjusted period returns, same-buys-in-S&P benchmark, money-weighted return), remove Wealth Projector + Exchanges, balanced Insights grid, .no
- **2026-07-10** · **v7.4**: restore code accidentally deleted in v7.3 (Markets search, stat-chip explainers, stock sheets) + add Watchlist (star stocks from any stock sheet, live prices on Markets, 
- **2026-07-10** · **v8.0**: full makeover — Portfolio · News · Explore · Insights
- **2026-07-10** · **v8.1**: ranked news + chart y-axis + holdings sort
- **2026-07-10** · **v8.2**: wealth-manager pass — benchmark scoreboard, drawdown chart, day attribution, deep holding stats, narrative Insights
- **2026-07-10** · **v8.3**: fair benchmarks, dividend calendar, CSV export, swipe-to-dismiss sheets
- **2026-07-10** · **v8.4**: live search prices, 52-week range bar, tappable performance rows, cumulative contributions line
- **2026-07-10** · **v8.5**: privacy mode (eye toggle) + per-holding news feeds
- **2026-07-10** · **v9.0**: vault — passcode + Face ID lock, AES-256 encryption, personal data removed from public source
- **2026-07-29** · **v9.1.0**: institutional upgrade — multi-language engine (EN default, DE, ES), dynamic multi-account filtering (Taxable, Roth IRA, 401k, Crypto, Cash), rebalance calculator with share counts, technical indicators (SMA 50/200 & trend signals), native iOS haptic bridge.
- **2026-08-04** · **v9.7.2**: fix — desktop rail nav (`.rail-nav__item`, ≥1024px) had no click handler; only `.tabbar__item` was bound, so left-rail navigation did nothing on wide viewports. Both now share one handler (haptics, view transitions, `TAB_ORDER` direction, heading focus). `showPage()` also now keeps `aria-current="page"` in sync on every nav button, not just `.on`, fixing a related bug where "Home" stayed permanently highlighted underneath the real active tab. No financial maths touched.
- **2026-08-05** · **v9.7.3**: Phase 5 vault audit fixes — `exportBackup()` no longer includes `pt_bk` (was exporting the raw cloud-backup key + passcode verifier alongside plaintext holdings); `importBackup()` no longer accepts a `bk` field from a backup file at all. Passcode minimum raised 6→8 chars and all-numeric passcodes rejected, enforced once in `js/vault.js` (`passcodeError`) for both first-time setup and `vaultChangePass`. PBKDF2 iteration count (310,000) untouched. No financial maths touched.
- **2026-08-05** · **v9.7.4**: Content-Security-Policy added via `<meta http-equiv>` in `index.html` — the only delivery path on GitHub Pages, which sets no response headers, so there is no `frame-ancestors`/`report-uri`. `connect-src`/`img-src` derived from `js/api.js` and `js/seed.js`'s real fetch/image origins. Two real footguns found and fixed, not just papered over: `js/app.js`'s `haptic()` used `style.cssText` (blocked under `style-src` without `'unsafe-inline'`); `js/core.js`'s `badgeHtml()` built `onload=`/`onerror=` as literal HTML attributes on an innerHTML'd `<img>` (blocked under `script-src` with no `'unsafe-inline'`, even though the handlers are JS-authored, not markup-authored) — replaced with two capture-phase `document` listeners keyed off the `.blogo` class. `js/tappable.js`'s JS-injected `<style>` tag moved to a static rule in `css/components.css` instead of granting `style-src-elem 'unsafe-inline'`. `style-src-attr 'unsafe-inline'` is still required and kept: ~82 sites across portfolio.js/insights.js/explore.js/sheets.js compute per-render inline `style=""` (bar widths, hash-derived badge gradients) that neither a nonce nor a CSP hash can cover, since the values differ every render. Verified empirically — zero `securitypolicyviolation` events across Home, Markets (search + stock sheet), Portfolio (holding-detail sheet), Insights (module sheet), theme toggle, and both demo-mode and real-passcode unlock/setup flows, including the WebAuthn/Face ID enrollment prompt. No financial maths touched.
- **2026-08-05** · **v9.7.5**: session close-out — C-1 fixed: `js/vault.js`'s `#unlockPass`, `#setPass1`/`#setPass2` and `#restorePass` now clear on the success path too (previously only on failure), so a real unlock no longer leaves the plaintext passcode sitting in the live DOM for the rest of the tab's life; verified empirically against a real (non-demo) unlock, not by reading the code. `js/insights.js`'s `renderCoach()` now `esc()`s `x.title`/`x.detail` before they reach `innerHTML` — local-only today, defence-in-depth. Added `test/export-import.spec.js`: the export/import round-trip had zero prior coverage and is the one path where a bug loses the whole portfolio; it also asserts the exported JSON carries no `bk` key, the TP-1 regression guard. No financial maths touched.
- **2026-08-05** · **v9.7.6**: three bug fixes, no new features. Logo/monogram stacking — `css/components.css`'s crossfade rule targeted `.blogo img.on`, a descendant-img selector that never matched `badgeHtml()`'s actual markup (`.blogo` lives directly on the `<img>`, since its very first commit) — fixed to `.blogo`/`.blogo.on`; verified pre-load opacity is 0 and a row with the logo host blocked still shows the monogram intact. Hero chart lines thicken then snap back after a Portfolio tab switch — measured a genuine ~30-130ms bitmap:CSS-size mismatch in Lightweight Charts' `autoSize` recovery from a hidden container (vendor-internal; manual `resize()`/`applyOptions()` calls only relocated the transient), so `showPage()` now veils `#mainChart` for a verified 220ms past every measured settle time. Two range chips lit at once — `#metricSeg`/`#rangeSeg` synced only the `.on` class, never `aria-selected`, so the hardcoded default stayed lit forever alongside whatever was actually picked (same class of bug as the rail-nav fix, commit `1b374e9`); verified exactly one chip lit after every click. No financial maths touched.
- **2026-08-06** · **v9.8.0**: Home v2 §1 — Daily Movers bar chart (`index.html`, `js/app.js`, `css/tokens.css`, `css/components.css`) replaces the old text-row "Today's movers" list and moves to the top of Home, above the portfolio card. Divs and CSS, no chart library: up to five columns on a shared baseline, a working gainers/losers toggle (`#moverToggle`), bar height proportional to the largest absolute move in the visible set and floored at `--mv-bar-min` so a near-zero move still shows a visible sliver, `--gain`/`--loss` gradient fill with rounded top corners only, sign always printed. `#moverTotal`/`#moverNarrative` retired with the old section — the new header ("Stay on top" / "Your Daily Movers") has no slot for them. Also: `badgeHtml()` now renders in the asset-detail sheet head (`js/sheets.js`, `js/portfolio.js`) — both showed plain ticker text with no logo before; the v9.7.6 chart-settling veil is now `--dur-chart-settle` (`css/tokens.css`) instead of a literal `220` in `js/app.js`. Added `test/active-state.spec.js`, a regression guard for the "hardcoded active state never cleared" bug class fixed three times now (rail-nav, `#metricSeg`/`#rangeSeg`, and now `#moverToggle`, built correct from the start) — asserts exactly one element lit across tabbar, rail-nav, `#rangeSeg`, `#metricSeg` and `#moverToggle` after every possible click. No financial maths touched.
- **2026-08-06** · **v9.9.0**: Home v2 §3-5 (`index.html`, `js/app.js`). Price highlights — top 3 holdings by lifetime total return (`holdingRow()`'s own `pl/cost*100`, not new maths), logo/ticker/name/return%, heading "Best performers" reads as observation; a portfolio with no holding in the green says so plainly instead of surfacing its least-bad loser. Portfolio insights — three tiles reusing `js/insights.js`'s `#modGrid` `.stat` component as-is (health grade, XIRR, vs VOO), each replaying the Insights tabbar button's own click to navigate there. `#moverNarrative` ("Ahead of/Behind the S&P 500 today") restored as this section's lead line next to the vs-VOO tile — deleted three times across R1/R4/the v9.8.0 movers session because each new movers header had no slot for it; it now has a permanent one. `#moverTotal` was not restored: it duplicated `#homeToday`'s day-dollar figure exactly, so it stayed genuinely redundant rather than orphaned. Also: `#screenSeg` (Markets screener — Active/Gainers/Losers) wired for the first time since R3 first collapsed the three lists into one segmented card; it rendered for three releases with no click handler. `test/active-state.spec.js` extended to cover it, including a real-effect assertion (which screener panel is visible), not just the lit-count invariant the rest of that suite checks. No financial maths touched.
- **2026-08-07** · **v9.10.0**: Home v2 §2 — "Coming up" now merges confirmed earnings dates into the dividend list (`js/app.js`'s `renderComingUp()`), one date-sorted list instead of dividends only. New Worker route `GET /earnings` (`worker/src/earnings.js`, wired in `worker/src/index.js`) proxies EarningsWhispers' own undocumented `api/getstocksdata/{ticker}` endpoint (found by reading their site's `stocks.js` — same unofficial/personal-use footing this repo already accepts for Yahoo, see `DATA-SOURCES.md`), KV-cached 24h per symbol so a portfolio doesn't hit upstream on every app open. Rows only render when the source's own `confirmDate` is set — an unconfirmed/estimated date is never shown, so ETFs (which the source returns nothing usable for) and stocks without an officially-announced date simply don't produce a row, same as a fund that pays no dividend wouldn't. Yahoo's `quoteSummary` `calendarEvents`/`earnings` modules and Nasdaq's public calendar were both tested and ruled out first — Nasdaq's date-based calendar has no per-symbol filter, and Yahoo's crumb-gating on those modules couldn't be confirmed either way (this environment was IP-rate-limited by Yahoo across the board at test time, including the already-verified `v8/finance/chart` baseline). New client state `state.earnings` (`js/core.js`) + `fetchEarnings()`/`ensureEarnings()` (`js/portfolio.js`) mirror the existing `state.divs`/`ensureDivs()` pattern exactly: 24h client TTL, called from the same two sites `ensureDivs()` already is. A failure in either feed doesn't touch the other. No financial maths touched.
- **2026-08-08** · **v9.10.1**: Phase 4 accessibility half — WCAG 2.2 AA audit against a real `@axe-core/playwright` run (not markup review), added as `test/a11y.spec.js` (Home/Markets/Portfolio/Insights/Following + the holding-detail sheet). Baseline: 119 violating nodes across 7 rules (6 critical `label`, 10 serious `color-contrast`/`aria-dialog-name`, 102 moderate `landmark-one-main`/`region`/`scope-attr-valid`, 1 minor `empty-table-header`). After: 0 violations on all six surfaces, all still green in this same suite. Fixes: `.wrap` is now `<main>` (`index.html`) — this alone cleared the bulk of the `region` count; the floating demo-badge/minibar/pull-to-refresh/assistant cluster grouped into a `role="region"` landmark since none of nav/main/header/footer fits it. New `--primary-text` token (`css/tokens.css`, dark theme only — light already cleared 4.5:1) fixes four text-on-tint combinations that measured 3.9–4.0:1 (`.chip--primary`, the active tabbar/rail-nav item, `#benchBtn.on`), same pattern `--primary-fill` already used for button text. Monthly-returns heatmap (`js/insights.js`) — year cells are now `<th scope="row">` instead of `scope` on a `<td>` (invalid), corner cell gets a visually-hidden "Year" label, table gets a visually-hidden caption. `js/sheets.js`'s `showOverlay`/`hideOverlay` (the one chokepoint every sheet opener already went through) now derives the dialog's `aria-label` from its own rendered `.sheet__title`, sets `inert` on the three background landmarks while a sheet is open (so Tab can't leave it — no hand-rolled keydown trap needed) and restores focus to whatever opened the sheet on close; found and fixed a swipe-dismiss path (`js/app.js`'s `wireSheetDrag('editSheet', ...)`) that bypassed `hideOverlay` entirely and would have left the app permanently `inert` after a swipe-close. Touch targets: `.allocstrip` (7px bar) and `#accChips` account chips (17px text buttons) get an invisible hit-area expansion to `--tap-sm` (24px) — same technique Phase 3 already used for `#tgtEditLnk`/`#goalEdit`/`.cq` — with zero visual change; `.search input` stretches to its 44px container. Eight chart canvases (`#mainChart`, `#detailChart`, `#allocChart`, `#divCal`, `#ddChart`, `#contribChart`, `#projChart`, `#worthChart`) get `role="img"` and a computed `aria-label` built from the same numbers each chart already draws; `#homeSpark` gets `aria-hidden` (redundant with `#homeTotal`/`#homeToday` beside it). The synthetic haptic-trick `<input switch>` (`js/app.js`) is now `aria-hidden`/`tabindex="-1"` — it was never meant to be perceived by anyone. `#demoBadge`'s `aria-label` ("Exit the example portfolio") didn't contain its visible text ("Example data") — Lighthouse's `label-content-name-mismatch` audit (WCAG 2.5.3, an axe rule tagged `experimental` so the default `@axe-core/playwright` run in `test/a11y.spec.js` didn't catch it) — now `aria-label="Example data — exit"`. Two accessible-chart helpers, `uiChart()`/`uiSpark()` in `js/ui.js`, looked unused from the app's own files — **correction, session 2**: they're called from `dev/components.html`, the maintained style guide, so "never called" was wrong; see that entry. Lighthouse accessibility (mobile viewport, Portfolio tab unlocked via the demo passcode, `lighthouse.snapshot()` against a live puppeteer-core page — a plain `lighthouse <url>` run only ever sees the lock screen): 90 → 100, measured against an isolated `git worktree` at the last commit vs. the working tree, not a guess. Not done, flagged rather than worked around: `.hrow`/`.mvcol`/allocation-legend rows are click-only `<div>`s with no keyboard path at all (no `tabindex`, no `role`, no key handler) — real, axe-can't-catch-it gap, touching it means retrofitting ~10 dynamically-rendered row templates across three files, out of this session's scope. Sheet/card titles (`.sheet__title`, `.section__label`, `.card__title`) are styled `<div>`/`<span>`, not real headings — a heading-list screen-reader user won't find them, though the sheet case is mitigated by the new `aria-label` announcing the name on open. Core Web Vitals and the Chart.js lazy-load are explicitly a separate session (UPGRADE_PLAN.md Phase 4). No financial maths touched; Phase 0 suite green throughout, golden master unchanged.
- **2026-08-08** · **v9.10.2**: Phase 4 accessibility, second half — keyboard operability and semantic headings, the two things v9.10.1's axe-clean pass could not see. **Keyboard**: `.hrow` (`js/portfolio.js`), `.mvcol` (`js/app.js`), `.drow` (`js/app.js`), every `.mrow` site (`js/explore.js` ×4, `js/app.js` ×1), `.alg`'s allocation-legend rows (`js/portfolio.js` — `#allocLegend` only, `#worthLegend`'s stay plain since they're non-interactive chart-legend swatches, not rows), and the `.stat` module tiles (`js/insights.js` `#modGrid`, `js/app.js` `#homeInsights`) are real `<button type="button">` elements now, not click-only `<div>`s — Tab reaches them and Enter/Space activates them for free, no keydown handler to write or forget. Verified CSS-safe per class before converting, not assumed: `.hrow`/`.mrow`/`.drow`/`.mvcol`/`.stat` already had an explicit non-default `display` (or are grid/flex children, which CSS blockifies regardless of the element's own display) and `base.css`'s existing `button, input, select, textarea { background:none; border:none; font:inherit; color:inherit }` reset already strips the one thing that would have differed visually; `.alg` was the one exception — no explicit display, so a bare button default (`inline-block`) would have laid dot+ticker+percent rows side by side instead of stacked, fixed with an explicit `display:block` alongside the tag change. Focus-visible needed no new CSS — `base.css`'s existing global `:focus-visible { outline: var(--ring) solid var(--primary) }` already applies to any real button; verified visible in both themes with an actual keyboard `Tab` (not a scripted `.focus()`, which Chromium's `:focus-visible` heuristic suppresses right after a mouse interaction — confirmed via `el.matches(':focus-visible')` before trusting a screenshot). `spark()` (`js/core.js`) — a live per-row sparkline missed in v9.10.1's canvas/`uiSpark()` sweep because it's SVG under a different name — now `aria-hidden`, redundant with the row's own value/return% text next to it. **Headings**: every `.sheet__title` (12 render sites across `js/sheets.js`, `js/portfolio.js`, `js/insights.js`) and `.section__label` (11 sites, `js/portfolio.js` + 10 static in `index.html`) is a real `<h2>` now instead of a styled `<div>`; `.card__title` was already `<h2>` everywhere (static markup and `js/ui.js`'s card builders) — nothing to fix there, the brief's premise was only half right. Both converted classes needed an explicit `margin: 0` added (`.section__label` keeps its intentional `margin-bottom`) since a `<div>`'s zero default margin isn't a heading's — verified `0px`/`0px` (sheet) and `0px`/`12px` (section, matching the intended `--s-3`) via computed style, not eyeballed. The whole app now uses exactly two heading levels, h1 (one per tab, mutually exclusive since only one `.page` is ever un-hidden) and h2 (every section/card/sheet title) — zero h3–h6 anywhere, verified by grep, which makes a heading-order skip structurally impossible rather than something to keep re-checking by hand. **`uiChart()`/`uiSpark()`** (`js/ui.js`): looked dead from inside the app (zero call sites in `index.html`/`js/*.js`, same files v9.10.1's search covered) — turns out `dev/components.html`, the maintained component style guide ("if a component is not in here it does not exist"), calls both. Deleting them broke it (`ReferenceError: uiChart is not defined`, caught by `test/smoke.spec.js`'s console-error assertion, not by inspection) — reverted, confirmed zero net diff on `js/ui.js`. Correct call is to leave both defined and still unused by the shipped app: the app's real chart canvases already get *dynamic* `role="img"`/`aria-label` pairs computed fresh on every redraw (v9.10.1), which is what they need since their data changes on every price tick and range switch — `uiChart()`'s label is set once at markup-build time and would go stale, so wiring the app's charts through it would be a regression dressed as a fix, not an improvement. `test/a11y.spec.js` extended with three keyboard-path tests, written and confirmed failing (`toBeFocused()` correctly fails on a plain `<div>` — focus() is a no-op on a non-focusable element, not a shortcut around the requirement) before the conversion, per TDD: Tab-reachable holdings row + Enter opens the detail sheet, a movers column + Space, a module tile + Enter opens its Insights sheet. Re-ran the full axe pass afterward: still 0 violations, 6/6 surfaces. Full Phase 0 suite green, golden master unchanged. Not done, flagged rather than worked around: `#sectorRow`'s `.krow` rows (`js/explore.js`) carry `data-sym`/`data-name` as if clickable but have no click handler at all, wired or delegated — equally broken for mouse and keyboard, so out of scope for a keyboard-operability pass specifically, but a real "looks tappable and isn't" gap `js/tappable.js` (built for exactly that bug class) missed. `#healthCard`'s "Portfolio health", `#hmCard`'s "Monthly returns" and `#sectorCard`'s "Sector exposure" (Insights tab) function as card titles but use `.stat__label`/`.hg`, not `.card__title`/`.section__label` — same class also labels non-title data points (`.stat__label` on "XIRR", "Beta", etc.), so a blanket conversion would have heading-spammed those; left as divs rather than risk it without a closer per-instance pass. `js/tappable.js`'s `#ideaList .mrow`/`#watchList .mrow` delegated click handlers look redundant with `js/app.js`'s page-level `.mrow` delegation (both listen for `click` on the same rows) — pre-existing, unrelated to this session's changes, not investigated further. Core Web Vitals and the Chart.js lazy-load remain the next session's scope. No financial maths touched.
- **2026-08-08** · **v9.10.3**: Phase 4 close-out — Core Web Vitals and the Chart.js lazy-load, the two items both prior Phase 4 sessions deliberately deferred. Measured on a mid-tier mobile profile (4x CPU throttle, ~150ms RTT / 1.6Mbps, matching Lighthouse's own mobile default) via `lighthouse`'s `startFlow()` — `navigate()` for real first-load metrics (this legitimately includes the lock screen: the vendor chart scripts loaded via blocking `<script>` tags regardless of lock state, so first-load weight was honestly measured there, not a repeat of the "plain URL only sees the lock screen" trap the accessibility sessions found) plus a `startTimespan()` covering unlock → tab switches → opening a chart-heavy sheet for interaction metrics, cross-checked against a direct in-page `PerformanceObserver({type:'event'})`/`{type:'longtask'})` reader since Lighthouse's own `interaction-to-next-paint` audit came back "not applicable" against synthetic Puppeteer clicks (tooling gap, not investigated further — the native Event Timing data it's supposed to summarize was present and readable directly). **Before → after**, first load: performance score 0.73→0.86, LCP 4.5s→3.7s, TTI 4.5s→3.7s, page weight 709→515 KiB, CLS 0.16→**0**. **Lazy-load** (`js/boot.js` `ensureChartJs()`): Chart.js (~200KB) is no longer a blocking `<script>` tag or in `sw.js`'s `CORE` precache array — it's injected on first actual use (holding sheet, allocation donut, or any Insights chart sheet) and left to `sw.js`'s existing generic cache-first-then-network handler to runtime-cache from then on, same offline guarantee after first use, just not paid for on installs that never open a chart-needing sheet. `boot.js`'s one-time Chart.js setup (font defaults, tooltip shape) and `js/portfolio.js`/`js/insights.js`'s `scrubLine`/`centerTxt` plugin registrations — all previously unconditional at parse time — moved into `ensureChartJs()`'s `onload`, since they'd otherwise silently never run once Chart wasn't guaranteed present at boot. Every call site (`js/portfolio.js` ×3 — `renderAlloc()`, `openDetail()` ×2 branches; `js/sheets.js` `openStockSheet()`; `js/insights.js` ×4 sheet openers) awaits the loader immediately before its existing draw call, re-checking the target canvas still exists afterward (a slow load can outlast a sheet close+reopen) — everything else in each of those functions stays exactly as synchronous as before, so nothing non-chart-related waits on a 200KB fetch. Both traps named in the brief, tested not assumed: `test/chart-lazy-load.spec.js` proves `window.Chart` stays `undefined` through unlock and a tab switch (real deferral, not just reduced page weight), that opening a holding sheet loads it and paints the canvas, and — the one that actually matters — a cold-offline-first-use run (`page.route` aborts only `vendor/chart.umd.min.js`, nothing else, simulating "never fetched online, nothing cached yet, now offline") shows the pre-existing "Connect to the internet once to load the chart library." fallback with no uncaught error and the sheet's non-chart content (price, stats) still rendering — the failure path already existed in `drawChart()`'s `!window.Chart` guard, this just proves it now covers a case (first-ever offline use) it never used to be reachable from. **CLS root cause** — named via a `layout-shift` `PerformanceObserver` logging DOM state at the exact shift, not guessed from the brief's font-swap hypothesis: `.lock__box`'s own text (`font-size-adjust: from-font` on `body`, `css/base.css`) contributed a real but tiny 0.00014 of the total; the actual 0.153-of-0.16 shift was `js/vault.js`'s `boot()` asynchronously deciding which `.lock__step` to show (existing vault vs. first-run setup vs. Face ID) — every step starts `hidden` until that resolves, so `.lock__box` (grid-centered via `place-items:center` in a fixed full-viewport container, so ANY height change repositions the whole box) grew from ~273px to as much as ~628px once a step revealed. Fixed with `--lock-box-min-h: 620px` (`css/tokens.css`) reserving space up front plus `align-content:center` so the logo/title stay centered in that space before a step fills it in, and a smaller `min-height` on `.lock__sub` itself (`css/components.css`) for the placeholder-text-to-real-message swap within that space. Verified against the same observer: navigation-phase CLS is 0 in the full Lighthouse re-run, not just "smaller." **INP / long tasks — named, then found not to hold up**: an initial `flow.startTimespan()` run showed a strikingly consistent ~1000ms long task at the same relative point on every run (3/3), which would have been a clean target for `scheduler.yield()` — except a CPU profile of that exact window (`Profiler.start()`, no Lighthouse tracing) showed 95%+ idle time with under 15ms of real JS self-time, and a parallel run using the browser's native Long Tasks/Event Timing APIs with Lighthouse's tracing removed entirely showed no task anywhere near 1000ms, ever (real tasks: 53–111ms; worst interaction 104–160ms, already under the 200ms target). Conclusion, evidence in hand rather than assumed: the 1000ms figure was Lighthouse's own tracing instrumentation competing for the main thread, not application code — it persisted identically before AND after every other fix in this session, which a real app-code bottleneck would not. Per the brief's own instruction to name a specific long task before reaching for `scheduler.yield()`, and having been unable to find one that survives a tracing-free measurement, none was added — sprinkling yields to move a symptom that isn't real would be exactly the anti-pattern being guarded against. **Two dead controls, per the brief**: (1) `#sectorRow`'s `.krow` rows (`js/explore.js`) carried `data-sym`/`data-name` since their first commit with no handler anywhere, wired or delegated — added to `js/app.js`'s existing page-level `.mrow, .idx-card` delegation selector (now `.mrow, .idx-card, .krow`), the same mechanism `.idx-card` already used, rather than a new one-off handler. (2) `js/tappable.js`'s `#ideaList .mrow`/`#watchList .mrow` blocks were confirmed — not assumed — redundant with `js/app.js`'s page-level `#page-markets`/`#page-following` delegation: instrumented `openStockSheet` to count calls, one click on an idea-list row called it twice (neither listener calls `stopPropagation`, both listen for `click`, both fire on bubble). Removed the two redundant blocks from `tappable.js`; re-instrumented, confirmed exactly one call. Both regression-guarded in new `test/dead-controls.spec.js`, per the brief, so a fifth dead control gets caught by a test rather than a person. Full Phase 0 suite green (30/30, including the two new test files this session added — `test/chart-lazy-load.spec.js`, `test/dead-controls.spec.js`), golden master unchanged. No financial maths touched.
- **2026-08-08** · **v9.11.0**: Phase 6 (final) — Monte Carlo goal projection replaces the deterministic ETA. `js/monte-carlo.js` is a new pure engine (`runMonteCarloProjection()`, tested Node-side with no browser in `test/monte-carlo.spec.js`): 10,000 seeded paths, real return N(7%,12%)/yr and inflation N(2%,0.8%)/yr drawn independently per path per year, contributions derived from the portfolio's own non-dividend lot history and deflated back to today's dollars by each path's own realized inflation. Zero-volatility case verified against closed-form compound growth exactly (both with and without contributions); the seeded generator's determinism is asserted directly. Runs in a new `js/monte-carlo-worker.js` Web Worker so the 10k-path computation never touches the main thread — the v9.10.3 session's 104–160ms worst-interaction measurement holds. `state.goal` (`js/app.js`) gains a required `year`; the four edge cases each render their own message instead of throwing — no goal, goal already reached, target year in the past, and fewer than 2 distinct purchase months to derive a contribution rate. Home's goal card now shows "N% chance by ⟨year⟩" plus a p10/p50/p90 fan chart (Lightweight Charts, already loaded for the hero chart — Chart.js stays off this path) and states its assumptions (return/inflation means+SDs, the derived contribution rate or its absence, pre-tax) inline. US-domiciled holdings: explicitly pre-tax, no German investment tax modeled. No existing financial maths touched; golden master (`test/golden`) doesn't cover goal/projection state, verified unchanged.
- **2026-08-09** · **v2.6.140**: version scheme replaced. Every prior entry above used a semver-shaped number (`vMAJOR.MINOR.PATCH`) that never actually meant MAJOR/MINOR/PATCH in the semver sense — this app has no public API to break, so "major" never meant anything more than "biggest one this session." The new scheme is **PROUD.NORMAL.SHAME**: first number bumps for a release the owner is genuinely proud of, second for a normal feature release, third for bug fixes and small things he'd rather not admit to. None of the three is a severity or compatibility signal — a large third-number jump means a lot of small embarrassing things got fixed in one sitting, not that anything is on fire. Set to `2.6.140` starting now: 2 prior "proud" releases, 6 normal feature releases, 140 accumulated fixes-and-small-things across the app's history to date — a deliberately honest count, not a reset to `1.0.0`. Touched: `sw.js` (`V`, cache-key prefix changed from `pt-v` to `pt-`, header comment explains the scheme so a later session doesn't "correct" it back to semver), `index.html` (`#appVer`), `native/project.yml` (`MARKETING_VERSION`, via `scripts/sync-version.sh`), `scripts/sync-version.sh` (regex updated for the new `pt-` prefix; the script itself still just enforces all three surfaces agree — it was never semver-aware and doesn't need to be). Every changelog entry before this one keeps its original vX.Y.Z number as written — not renumbered to fit the new scheme. **Same session, second change:** the `portfolio-push` Cloudflare Worker was redeployed under a different Cloudflare account (the account that owned `portfolio-push.vdrxppy99.workers.dev` and its KV namespace wasn't the one available this session) — new KV namespace created, `worker/wrangler.jsonc`'s `kv_namespaces[0].id` updated (binding name `KV` unchanged, so no worker source touched), deployed, and live-verified: `/earnings` returns real confirmed dates for tickers that have one (e.g. NVDA, WMT, TGT) and correctly-null for ETFs (VOO) and not-yet-confirmed stocks; `/backup` and `/restore` respond and enforce auth/lookup exactly as before. New live host is `portfolio-push.portfolio-push.workers.dev` — updated everywhere the old one appeared: `js/api.js` (`PUSH_URL`), `js/vault.js` (`PUSH_URL_V`, the pre-unlock duplicate), `index.html`'s CSP `connect-src` (missing this one specifically would have silently blocked every Worker call — CSP violations don't throw a catchable JS error), and `test/helpers.js`'s `EXTERNAL_HOSTS` network-block list. **Orphaned data, not migrated:** the OLD Worker's KV held any existing cloud backup (`/backup`'s `backup` key) and push subscription; none of that carries over to the new KV, and the old Worker/account may no longer be reachable to retrieve it. Anyone relying on a prior cloud backup should hold a local export (⚙︎ → Export backup) before trusting the new one — this is expected to coincide with a separate cloud-key rotation, not a surprise. The Workers AI `/ask` daily-cap counter and the VAPID web-push keypair are also fresh/reset on the new account (`VAPID_PUB` carried over in `wrangler.jsonc`, but the matching `VAPID_PRIVATE_JWK` secret was never set on this account — push notifications will fail to sign until it is; not fixed this session, flagged only).
- **2026-08-09** · **v2.6.141**: hero chart cold-load thickening, fixed at the root instead of veiled. The v9.7.6 fix only covered `showPage()`'s tab-switch path with a 220ms opacity veil (`--dur-chart-settle`); the regression came back because `renderAll()` draws the hero chart unconditionally at boot, before Portfolio (not the landing tab since R4) is ever opened, i.e. against a 0×0 `#mainChart` — a path the veil never touched. `js/portfolio.js`'s `drawHeroChart()` no longer uses Lightweight Charts' `autoSize`: if the container measures 0×0 at draw time, chart creation is deferred behind a `ResizeObserver` until the container has a real size, and every later resize is driven by our own `ResizeObserver` calling `chart.resize()` explicitly — one deterministic size transition instead of relying on the vendor bundle's own internal autoSize/ResizeObserver recovery timing. Verified by sampling every visible chart canvas's `width`/`height` against its own `getBoundingClientRect()` every animation frame (not by screenshot) across cold load, tab-switch-away-and-back, theme toggle, every range button, and devicePixelRatio 1/2/3: the pre-fix code produced a canvas bitmap scaled by a full extra devicePixelRatio factor on one axis relative to the other (e.g. ratioX 1 vs ratioY 3) specifically on cold load and on tab revisit; post-fix, neither case ever does — confirmed against an isolated vendor-only reproduction first, since this headless Chromium's `ResizeObserver` doesn't report `devicePixelContentBoxSize`, so literal bitmap-equals-CSS×dpr is unachievable here even for a correct chart, before or after the fix, and isn't what this regression actually is. The `chart-settling` veil, `.chart-settling` (`css/components.css`) and `--dur-chart-settle` (`css/tokens.css`) are removed — nothing needs to hide behind opacity now that no size transition happens against a wrong starting size. New `test/hero-chart-resize.spec.js` (2 tests) asserts this directly on cold load and on tab revisit; confirmed both fail against the pre-fix code with the exact `ratioX≈1, ratioY≈devicePixelRatio` signature described above, and pass against the fix. Full suite green (39/39), golden master unchanged. No financial maths touched.
- **2026-08-09** · **v2.6.142**: the PWA and the native iOS build had byte-identical icons — same file, both predating the indigo rebrand (still the old green line-chart glow) — so there was no way to tell which install you were opening from a home screen. Both are now the same mark (an ascending sparkline with one dip and a marker dot, kept within an ~80% safe zone so a maskable/circular crop never clips it) recolored to the current brand (`--brand` `#7C6CFF`), but with an inverted treatment: the PWA (`icon-192.png`, `icon-512.png`, `apple-touch-icon.png`) keeps the app's own dark-canvas-with-glowing-indigo-line identity (`#0B0F17` background); the native build (`native/Portfolio/Assets.xcassets/AppIcon.appiconset/AppIcon.png`, single 1024×1024 — this asset catalog already uses Xcode's modern single-universal-size format, no legacy size matrix needed) is a solid indigo fill with a flat white line, no glow. Checked at the size that actually matters, 60px on a home screen, not 512: background color is the strongest signal that survives shrinking (fine glow/line detail doesn't), so near-black-with-glow vs. solid-indigo-flat reads as two distinct apps at a glance, side by side, where two different line-wiggle styles would not have. Generated with a small PIL script (Catmull-Rom spline through six keypoints, no new project dependency), not hand-drawn per file, so every size is the same vector-equivalent shape rather than four independently-drawn approximations. `sw.js`'s `CACHE_NAME`/`V` bumped so installed PWAs actually fetch the new files instead of keeping the old ones forever (`icon-192.png`/`icon-512.png`/`apple-touch-icon.png` were already in `CORE`, no array change needed). No content, layout or financial maths touched.
- **2026-08-09** · **v2.6.143**: push notifications, part 1 (arrival, not scheduling) — the client subscribe flow (`js/api.js`), the Settings toggle + "Send test now" button (`js/portfolio.js`), and the `sw.js` `push`/`notificationclick` handlers were all already built in an earlier, undocumented session; what was actually broken was exactly what v2.6.140's entry above flagged and left unfixed: `VAPID_PRIVATE_JWK` was never set on the current Cloudflare account, so every `vapidAuthHeader()` call would throw and no push could ever sign. Before regenerating a keypair as originally planned, checked `worker/.dev.vars` — it held a private JWK whose EC point matched `VAPID_PUB` (the one already live in `wrangler.jsonc` and hardcoded in `js/api.js`) byte-for-byte; it wasn't lost, just never pushed as a secret. Set `VAPID_PRIVATE_JWK`, `VAPID_SUB`, and `ADMIN_TOKEN` via `wrangler secret put` on the current account and redeployed `portfolio-push`. Verified what's verifiable without a phone: the deployed private key signs and its own signature verifies against the deployed public key (WebCrypto round-trip); `worker/test/vector.mjs` still passes both the RFC 8291 encryption vector and a live VAPID JWT sign/verify against `webpush.js`'s actual code; the Worker responds 200 live. Separately, `js/api.js`'s `pushEnable()` only explained the "iOS asks once, a denial needs delete-and-reinstall to fix" risk *after* a denial — added a `confirm()` before the first-ever `Notification.requestPermission()` call so the owner sees that warning before, not after, tapping Allow/Don't Allow. Not verified and not fixable from here: actual delivery through Apple's push relay to a subscribed phone — that needs the owner's own Home Screen install. Full suite green (39/39), golden master unchanged; no financial maths touched. Part 2 (scheduled/triggered sends — the existing daily-report crons and mover alerts in `worker/src/index.js`/`alerts.js`) is untouched this session, deliberately. **Separate cleanup session, same day:** `native/Portfolio/PortfolioApp.swift`'s WebView background was still the pre-Phase-1 `#0b0f0d` canvas color — missed when the web canvas was retargeted to `#0B0F17`, visible as a seam/flash behind the WebView on native launch — now matches; the rest of the native shell (`Biometrics.swift`, `Dialogs.swift`, `WebScreen.swift`) has no other hardcoded colors. `native/build/` (Xcode's generated output, ~3,000 files) was tracked in git, bloating every diff — added to `.gitignore` and untracked with `git rm -r --cached` (files left on disk). `native/Portfolio.xcodeproj/project.pbxproj` regenerated via `xcodegen generate` from `native/project.yml` so its `MARKETING_VERSION` stops lagging behind the web version. Stress-tested the suite for the parallel-worker-dependent flaky test an earlier session reported (default 4 workers, then `--workers=8`, then `--workers=8 --repeat-each=3` — 156 total test executions): 100% pass rate throughout: could not reproduce, so it isn't named here — flagged instead of guessed.
- **2026-08-09** · **v2.6.144**: push notifications, part 2 (scheduled sends). Before building, checked what's actually deployed: the cron-triggered daily open/close reports, intraday movers, ATH/ATL, band breakouts, big-portfolio-day, dividend-paid, dividend-ex-date reminder, milestone, goal, and price-target alerts (`worker/src/alerts.js`, `worker/src/index.js`) were already built and already cron-triggered — `worker/wrangler.jsonc`'s five existing crons are reused as-is, not duplicated with a new one. The only genuinely missing notification type was earnings-tomorrow. Added: `worker/src/earnings.js`'s KV-cached lookup is now `fetchEarningsFor(env, syms)`, shared between the existing CORS-gated `/earnings` client route and a new check in `alerts.js` that fires once, the day before a directly-held stock's CONFIRMED earnings date (unconfirmed EarningsWhispers estimates are already dropped upstream, same rule the `/earnings` route already enforced); ETFs return null from the source and are silently skipped. Opt-in and off by default (`⚙︎ → Earnings-day alerts`, `js/portfolio.js`) per the session's noise bar ("would he act on it or want to know it") — reaches the Worker via a new `earningsAlerts` boolean on the existing snapshot sync (`pushSnapshot()`, `js/api.js`), no new endpoint. **Flagged, not silently built around:** this session's brief assumed the Worker only ever sees ticker symbols; it already receives share quantities, cash, deposits, goal amount and price-target values (`pushSnapshot()`, built and user-approved in an earlier, undocumented session) — surfaced to the owner, who chose to keep the existing full-snapshot design over gutting the already-shipped $-amount alerts. `README.md`'s Data section is corrected accordingly — it previously and incorrectly claimed "ticker-only." Full suite green (39/39), `worker/test/vector.mjs` (RFC 8291 + VAPID) still passes; golden master unchanged; no financial maths touched. Worker deployed; not yet pushed to origin, pending the owner confirming the client-side toggle and a live earnings alert.
- **2026-08-13** · **v2.6.145**: Home's hero total went stale during live polling, found and live-reproduced in a prior bug-hunt session — `renderHomeCard()`/`renderHomeChrome()` were only ever called from the full `renderAll()`, but `refreshAll()`'s fast quotes-only phase (`js/api.js`) and `refreshQuotesOnly()`'s 10-60s market-hours poll each painted their own hand-written subset of surfaces that omitted them, so Portfolio's header kept ticking live while Home's total sat frozen at its last full render — on Home itself, the Daily Movers chart and the pinned mini-bar updated live right next to a frozen hero total. Root cause was the lack of one shared definition of "what must repaint on a quote change," not a missing function call: introduced `renderLiveSurfaces()` (`js/app.js`) as that single definition — `renderHeader`, `renderList`, `renderMover`, `renderHomeCard`, `renderHomePr`, `renderPriceHighlights`, `renderHomeInsights` — and both partial-render call sites (`js/api.js:141`, `js/api.js:185`) now call it instead of keeping their own list; `renderAll()` calls it too, so there is exactly one place to register a future live surface. Audited for other surfaces missing from the old partial lists while in there: found `renderGoal()` (goal progress card) also reads live totals but deliberately was NOT added to `renderLiveSurfaces()` — on a cache miss it dispatches a 10,000-path Monte Carlo run to a Worker, and a 10s poll tick should not be triggering that; it remains `renderAll()`-only, flagged as a separate, not-yet-fixed gap rather than silently pulled in. New `test/live-surfaces-consistency.spec.js` drives the real production fetch pipeline (mocks only VOO's network response, not `setQuote`/`fetchQuote` themselves) through the actual `refreshQuotesOnly()`, then asserts Home's total, Portfolio's total, and the mini-bar's total are the same string, and same for the day-change figure — confirmed failing against the pre-fix code (`Home total did not move after the live poll tick`) before the fix, passing after. Splash-screen color drift, the empty-portfolio health-score edge case, Home's missing staleness indicator, and the German-coverage gaps found in the same hunt session are untouched — separate, unrelated bugs, left for their own sessions. Full suite green (39/39), golden master unchanged; no financial maths touched.
- **2026-08-13** · **v2.6.146**: cleanup session, four more fixes from the same bug hunt plus one follow-up from v2.6.145's live-surfaces fix. (1) Theme colour had three independent hardcoded copies (`manifest.webmanifest`, `index.html`'s `<meta theme-color>`, and a third value in `js/app.js`'s in-app theme toggle) that had drifted from `css/tokens.css`'s `--canvas` and from each other — the same stale-`#0b0f0d` bug already caught and fixed once for the native shell in v2.6.143, missed on the web side. `index.html` now ships light AND dark `<meta name=theme-color media="(prefers-color-scheme: …)">` tags, both copied byte-identical from `--canvas`'s current values (comments in both files point at each other so a future `--canvas` change has an obvious paper trail — a JSON manifest and a static `<meta>` can't read a CSS custom property directly with no build step to inline it); `manifest.webmanifest`'s `background_color`/`theme_color` now match the icon's already-current `#0B0F17` (v2.6.142); `js/app.js`'s toggle now reads `cvar('--canvas')` instead of a hardcoded third value, so an explicit in-app choice overwrites both `<meta>` tags to whatever's actually active — no hardcoded literal left in JS at all. (2) `healthScore()` (`js/app.js`) scored an empty portfolio a confident B/84 — Diversification and Cost efficiency both read "no evidence of a problem" as "no problem" on zero holdings, Cash deployed did the same whenever cash was also zero; only Global exposure degraded sensibly. Added one guard (`!rs.length` → `{score:null, metrics:null}`) rather than patching each metric, since the actual defect is "health isn't a measurable concept for a portfolio that doesn't exist," not a wrong formula in one of four checks; all three UI call sites (`renderHealth()`, `renderHomeInsights()`'s stat tile, `openHealthSheet()`) now show "—"/"not enough data", matching the XIRR/vs-VOO tiles' existing convention for the same case. (3) Home (the landing tab) had no staleness/offline signal at all — `setStatus()` wrote only to Portfolio's `#status`/`#statusTx`. Added `#homeStatus`/`#homeStatusTx` (`index.html`, same markup/CSS as Portfolio's) and generalized `setStatus()` into computing the status once and painting it through a `STATUS_PAIRS` list (`js/portfolio.js`) — the same "one definition, every surface repaints from it" shape as `renderLiveSurfaces()`, so a third status surface is a one-line addition, not a second status system. (4) `renderGoal()` had the exact staleness bug `renderLiveSurfaces()` fixed in v2.6.145, deliberately left out at the time because a cache miss dispatches a 10,000-path Monte Carlo run to a Worker. Split it: `renderGoalProgress()` (`js/app.js`) patches the goal card's "$X of $Y" figure and progress-bar width in place from the live total and is now in `renderLiveSurfaces()`, so it runs on every 10s poll tick; the simulation itself still only runs from the full `renderGoal()` (`renderAll()`, or saving the goal-edit form). "Materially changed" wasn't given a new definition — it's the same tolerance the code already had (goal amount, target year, contribution rate, or portfolio value crossing the pre-existing $50-rounded memoization key), just evaluated at full-render frequency instead of every tick, which is what makes calling the cheap half from a tick safe. (5) Investigated `#tvNum`'s roll-up tween showing the old value on its first animation frame (flagged, not fixed, by the v2.6.145 session's test) — concluded it is NOT a bug: `renderHeader()` sets the new value, then `rollUpTvNum()` immediately starts a count-up tween FROM the previous value, which is the correct, intended behavior for a count-up animation (CLAUDE.md's Phase 3 log documents it as designed), and browsers never paint mid-script, so nothing is visibly shown out of order — the "flash" was a same-tick synchronous-DOM-read artifact specific to the v2.6.145 test (worked around there with `emulateMedia({reducedMotion:'reduce'})`), not something a real user sees. Left unchanged, documented here rather than forcing an unnecessary change to already-correct code. New `test/live-surfaces-consistency.spec.js` from v2.6.145 still covers (1)-(4)'s render paths; no new tests added this session (all five items are either presentation-only guards or the kind of thing the existing a11y/smoke suite already exercises going empty/live). Full suite green (40/40), golden master unchanged; no financial maths touched.
- **2026-08-14** · **v2.6.147**: German coverage, session 1 of 2 — architecture, the guard, and the static gaps (bug-hunt finding: `js/i18n.js`'s post-render dictionary+pattern pass can only ever translate a string that's byte-identical every time, so it systematically misses static boilerplate nobody added to the dictionary and any sentence built from a template literal with an interpolated figure). Architecture decision, stated before writing code (see `js/i18n.js`'s own header comment): the post-render pass isn't rewritten — kept exactly as-is for the ~162 strings it already covers — but a new `t()` tagged-template function is introduced for translating AT the render site going forward; new and fixed strings use it, existing working coverage doesn't get migrated to it. `t()` joins a template literal's static segments into one dictionary key (shared with the same `DE` object, not a second dictionary) so every instance of a sentence matches regardless of the interpolated value — session 2's actual job is migrating `js/insights.js`'s and `js/sheets.js`'s existing template literals to it; none of that is touched this session, deliberately, per brief. Built the guard first and watched it fail: `test/i18n-coverage.spec.js` switches to DE, walks all five tabs plus 18 reachable sheets (called directly via their global `open*Sheet()` functions, same convention `test/dead-controls.spec.js` already uses), and checks that no known-English string (dictionary keys + index.html's own static text, extracted from the source) appears verbatim — measured, not estimated: **23 leaks before this session's fixes, 16 after** (a ratchet, not a zero-tolerance gate — `MAX_KNOWN_LEAKS` is session 2's exact starting scope, must never silently rise). Two false-positive classes found and excluded from the candidate list by direct inspection, not guessed: "Total"/"Value"/"Home" as substrings of untranslatable proper nouns already in the demo data (fund names "Vanguard Total Stock Market ETF"/"Avantis US Small Cap Value ETF", and "Home Depot" in the look-through list) — the "Home" TAB LABEL's own fix was confirmed separately by direct DOM inspection instead. Fixed the 9 static gaps the hunt named: the "Not financial advice" disclaimer (4 distinct sentences, 5 tab instances — Markets and Following share one), "Total portfolio" → "Gesamtwert", "Search stocks and ETFs" → "Aktien und ETFs suchen", "Deposit-adjusted · updated today" → "Einzahlungsbereinigt · heute aktualisiert", "Sector exposure" → "Sektorengewichtung", and the "Home" tab label (the only one of five with no dictionary entry at all) → "Übersicht". Not literal translation: "Keine Anlageberatung" for "not (financial) advice" reuses this codebase's own existing established term (already used once, in the Markets "Ideas" caption) rather than inventing a second phrasing; "Gesamtwert"/"Aktien und ETFs suchen" mirror this app's existing analogous entries' conventions. Two terms flagged as reasonably-confident-not-verified rather than silently guessed: "einzahlungsbereinigt" (deposit-adjusted, following the standard German "X-bereinigt" pattern) and "Sektorengewichtung" (sector weighting, the term German fund fact sheets use) — both real, defensible German finance usage, neither checked against a native speaker or style guide. All 9 fixes live-verified in a browser (DE mode, all five tabs). Full suite green (41/41 — the new guard test plus the 40 existing), golden master unchanged; no financial maths touched.
- **2026-08-14** · **v2.6.148**: German coverage, session 2 of 2 — closed the remaining leaks, wired `t()` into every generated sentence that needed it. Before touching anything: found and fixed a real defect in `t()` itself, per the brief's explicit requirement to confirm this first — the original implementation joined a template's static segments on a bare `"%s"` and reinserted interpolated values purely by array position, so a German translation had no way to place its SECOND value before its FIRST (German clause order routinely differs from English). New `test/t-function.spec.js` (4 tests) proved this: watched 3 of 4 fail against the original implementation, fixed by numbering placeholders (`%1`, `%2`, …) in the derived key instead of a bare `%s`, so a dictionary VALUE can reference any interpolated value by number, in any order, repeated or not — confirmed all 4 pass after. Migrated every one of session 1's 16 measured leaks to either a dictionary entry (static strings a template literal happened to embed, e.g. "Cash · settlement fund", "Profit %", the "Erase all holdings" button/confirm-dialog trio, two long info-sheet paragraphs) or `t()` (genuinely dynamic: `js/app.js`'s market-open/close countdown, `js/portfolio.js`'s "· market closed" status suffix, `js/sheets.js`'s "Tax lots · all N" title, `js/insights.js`'s "Portfolio Health · N/100" title and its four-piece conditional dividend-growth projection sentence — each conditional piece its own `t()` call, concatenated as results, so the existing conditional composition logic is untouched). Two more leaks surfaced only once the count above dropped low enough to be visible against the noise: Markets-tab fetch-retry messages (`js/explore.js`, only shown when a fetch actually fails) and a second "Erase all holdings" string (the button, distinct from its confirm-dialog title) — both fixed the same way. **Leak count: 16 before this session, 0 after** — `MAX_KNOWN_LEAKS` in `test/i18n-coverage.spec.js` lowered to match; the ratchet now guards against any regression rather than measuring remaining scope. Not literal translation, same standard as session 1: "Keine Anlageberatung" reused again for "Not advice."; "Std."/"Min." for the countdown (standard German abbreviations, not English "h"/"m" carried over); a German-typography paragraph (`„…"` quotes, space before `%`) for the Portfolio P/E explainer. **Investigated, reported, not changed, per the brief:** number and date formatting are governed by neither the app's language setting nor a shared locale concept. `js/core.js` `fmtPx()`/`cfmt()` and three Chart.js tick-formatter call sites (`js/insights.js:187,806`, `js/portfolio.js:658,715`) all derive their `Intl.NumberFormat` locale from `state.view.ccy` (`'de-DE'` iff currency is EUR) — live-verified: language=DE + currency=USD (the app's own default) renders the total as `$153,959.57`, American-style, inside an otherwise fully German UI. Separately, every `toLocaleDateString()`/`toLocaleString()` call across `js/app.js`/`js/insights.js`/`js/portfolio.js`/`js/sheets.js` passes `[]` as the locale argument, which resolves to the BROWSER's own locale — live-verified as `en-US`, "Aug 14, 2026" — with zero connection to either the language or currency setting. Whether formatting should follow language, stay tied to currency, or become its own independent setting is a product decision, not made here. Full suite green (45/45 — the new `t-function.spec.js` plus the 44 existing), golden master unchanged; no financial maths touched.
- **2026-08-14** · **v2.6.149**: locale formatting now follows the LANGUAGE setting, not currency and not the browser's own default — decision made by the owner, implemented as one resolver rather than five-plus independent call sites (the same shape of defect as the three theme-colour copies in v2.6.147 and the missing `renderHomeCard()` call in v2.6.145: nothing made a sixth copy wrong to add). New `window.appLocale()` (`js/i18n.js`, next to `appLang()`) resolves `de`→`'de-DE'`/`en`→`'en-US'`; every formatter now routes through it — `fmtPx()`/`cfmt()` (`js/core.js`), three Chart.js tick-formatter callbacks and two compact-notation helpers (`js/insights.js:187,806`, `js/portfolio.js:658,715`), and eighteen `toLocaleDateString()`/`toLocaleString()` call sites across `js/app.js`, `js/insights.js`, `js/portfolio.js`, `js/sheets.js`, `js/explore.js` that previously passed `[]`/`undefined` (the browser's own locale, tied to neither app setting). `Intl.NumberFormat`'s `currency` option is untouched by any of this — locale controls separators/symbol position/date order, `state.view.ccy` (USD/EUR) still controls which symbol appears; the two were always independent inputs to the same API, only the locale was ever wired to the wrong signal. Five call sites deliberately left alone, not missed: `js/app.js`'s and `js/portfolio.js`'s NYSE-hours `formatToParts()` calls (parsing, not display — changing locale would break the weekday/hour lookups the market-open logic depends on), `js/insights.js`'s `askContext()` (an English-language prompt sent to the AI assistant, not something a user reads), and `js/portfolio.js`'s Siri briefing text + `u.lang='en-US'` (spoken aloud in English by design, unrelated to on-screen display). New `test/locale-formatting.spec.js` (5 tests) — written and watched fail against the pre-fix call sites first — asserts all four language×currency combinations produce the exact `Intl` output (including the U+00A0 no-break space `de-DE` puts before its currency symbol, verified against Node's own `Intl.NumberFormat` before writing the assertions, not guessed) and that EN+USD — the golden master's own combination — is byte-identical to the pre-fix string. All four combinations live-verified in a browser: EN+USD `$153,959.57`/"Aug 14, 2026", EN+EUR `€153,959.57`/"Aug 14, 2026", DE+USD `153.959,57 $`/"14. Aug. 2026", DE+EUR `153.959,57 €`/"14. Aug. 2026" — and DE+USD reloaded end-to-end (not just the formatter in isolation) to confirm German text and German number formatting render together correctly, not just each in isolation. Full suite green (50/50 — the new `locale-formatting.spec.js` plus the 45 existing), golden master unchanged; no financial maths touched.
- **2026-08-14** · **v2.6.150**: Insights v2, session 1 of 4 — palette fix, then Risk/Deepest-drop/Return-vs-benchmark. **Palette:** `css/tokens.css`'s `--cat-1..8` only ever contained six distinct hues (`--cat-2`/`--cat-7` both blue, `--cat-3`/`--cat-8` both orange, reading as different only through lightness, which the dark band forbids) — confirmed failing (`.claude/skills/dataviz/scripts/validate_palette.js`, both modes) before touching anything. Re-derived six slots — slot 1 is `--brand`, slots 2-6 a fixed hue order (yellow/magenta/orange/blue/aqua) found by enumerating orderings and keeping the one maximizing worst-adjacent OKLab ΔE, then a randomized L/C search per slot per mode against this app's own surfaces (`#0B0F17` dark, `#FBFBFC` light) — passes every check cleanly in both modes (worst adjacent CVD ΔE 16.5 dark/19.4 light, normal-vision ΔE 17.0/20.5, all ≥3:1 contrast, no relief needed): pasted report in the session transcript. Updated `css/tokens.css`, `redesign/contrast-check.mjs`'s `RAMP_DARK`/`RAMP_LIGHT`, and `dev/components.html`'s swatch gallery (all three `--cat-7`/`--cat-8` consumers). New `test/palette.spec.js` reads the LIVE computed `--cat-1..6` values from a running page in both themes and runs them through the same validator — written and watched fail against the old 8-slot palette first (`git stash` the tokens.css hunk, confirm RED with the exact documented failure reasons, `stash pop`, confirm GREEN), so a reintroduced collision can't ship silently again. `js/portfolio.js`'s `boot.js` `CAT` array (the allocation-donut colours) is a separate, independently-hardcoded 6-colour array that doesn't consume `--cat-*` at all — spotted, flagged, not touched (out of this session's scope). **Three modules rebuilt to match `design/target/insights-v2.html` (the approved rendered spec) exactly:** Risk — a single beta axis, S&P ticked at 1.00, all six holdings plotted and named, the portfolio marked distinctly; Deepest drop — the actual drawdown curve with the trough marked and dated, plus which holding fell hardest and which held up best; Return vs benchmark — paired bars (your XIRR vs the same buys replayed into VOO), not two stat tiles. No new maths: Risk's per-holding beta is `riskStats()`'s own existing cov/var-vs-VOO formula (`js/insights.js`), now also run per-symbol against the per-symbol daily-return series the function already built internally for the portfolio-weighted blend, not a second model. Deepest drop reuses `buildSeries('all')` and the same peak-tracking scan `riskStats()`'s `mdd` figure already runs (kept as a path instead of collapsed to one number) plus `pathValue()`'s existing binary-search-over-daily-closes technique for the per-holding peak→trough price move. Return vs benchmark's "same buys in VOO" annualized rate is `personalReturn()`'s own `xirr()` call and cashflow construction, terminal value swapped for `pathValue()`'s benchmark replay instead of the real portfolio total. `renderModGrid()` drops the four tiles these replace (XIRR, vs VOO, Beta, Max drawdown) — DESIGN-TARGET.md's rule ("a module that can only be a number is a stat tile, not an Insights module") — and keeps Volatility + Portfolio P/E, matching the approved spec's own bottom `grid2`. **Goal lines:** all three end on the indigo-rule sentence DESIGN-TARGET.md calls the signature element, derived from real state only (`state.goal.amt`/`.year`, `deriveMonthlyContribution`-adjacent real dollar figures) via a new `projectedGoalDate()`/`monthsToReach()` pair that reuses `renderFI()`'s own existing log-compound ETA formula (`years = ln(target/current)/ln(1+growthRate())`), retargeted at the owner's goal amount instead of the 4%-rule freedom number — not a new model. Each line is omitted (not filled with a placeholder) when the underlying state doesn't support an honest one (no goal set, or the shock/benchmark delta doesn't actually move the projected date). **A real bug found and fixed during live verification, not just tests:** the Risk axis's alternating up/down label placement (copied from the mockup, which uses illustrative, well-spread-out demo betas) collapsed into unreadable overlapping text against this portfolio's REAL betas, which cluster tightly (YOU/VOO/VTI/VXUS all within ~0.13 of each other) — strict alternation only guarantees immediate x-neighbors differ in row, not a 4-wide cluster where every-other item still shares a row. Fixed with a per-row decluttering pass (walk each row in x-order, push any label still within 24px of the previous one in that row rightward; circles stay at the true data position, only the text moves) — live-verified in a browser against the real demo portfolio's actual clustered betas, not just the mockup's spread-out ones. Dataviz anti-pattern check performed before writing chart code (`.claude/skills/dataviz/references/anti-patterns.md`), entries explicitly checked: dual-axis (none used), recolor-on-filter (N/A, no filtering), status-colour misuse (`--loss` on the drawdown curve is a correct status use — the curve literally IS a loss state; `--brand`/`--mut` on the paired bars and axis marker are identity, not status), dashed gridlines (only the S&P benchmark TICK is dashed — a value marker, not a structural gridline, which stay solid), border-around-marks (the portfolio marker's canvas-coloured ring is the documented 2px surface-gap ring technique, not the anti-pattern), label-in-too-small-a-mark (XIRR bar labels sit in the reserved gutter outside the bar, never inside/clipped), tabular-nums-on-a-hero-number (this app's `base.css` applies it globally as a pre-existing, documented, non-negotiable project rule for a different reason than the general anti-pattern — kept, not a violation). Tooltip-gating and table-view-twin checked: no charts here have hover tooltips at all, but every value is already reachable via a direct label, and each card's tap target opens the corresponding existing detail sheet (`openRiskSheet`/`openPerfSheet`), which is the table view. Sector/heatmap/health/"More" list untouched this session, as scoped — sessions 2-4 continue the remaining modules. Full suite green (52/52 — the new `palette.spec.js` plus the 50 existing), golden master unchanged; not pushed.
- **2026-08-14** · **v2.6.151**: Insights v2, session 2 of 4 — the remaining number-shaped modules: Contributions, Sector, Portfolio P/E. **Contributions ("What you added"):** bars over the last 12 real contribution months, replacing the flat total. Same monthly aggregation `renderContribChart()` already does (`state.lots` grouped by `date.slice(0,7)`), just the last 12 months instead of 24, drawn as hand-rolled SVG bars matching the approved spec instead of Chart.js. Goal line needed a genuinely new capability — relating a contribution RATE to an arrival date — that `monthsToReach()`'s existing "today's money alone" ETA (session 1) structurally can't do, since it has no contribution term by design (same convention as `renderFI()`/`renderProjection()`). The only model in this app that adds money over time is `js/monte-carlo.js`'s `runMonteCarloProjection()` (Home's goal card) — reused here at its already-unit-tested zero-volatility, one-path special case (`test/monte-carlo.spec.js`: "zero volatility with contributions matches a hand-computed annuity-with-inflation-decay sum"), so this is the same engine read deterministically, not a second model; linear interpolation between the two bracketing yearly fan points turns its year-granularity output into a month figure. Both the "arrive N months early" baseline (session 1's `projectedGoalDate()`, no contributions) and the "$100 more" scenario reuse this one new function (`monthsToReachWithContribution()`) with different inputs. **Sector ("Where the money actually is"):** categorical 8-colour allocstrip → sequential single-hue ramp ordered by magnitude — a form correction per `choosing-a-form.md` ("compare magnitude, low→high" is a sequential job; six categorical hues were spending the identity channel re-encoding what row order + label already show), stated before writing the chart code. Same `SECTOR_WEIGHTS` aggregation `renderSectorStrip()` (removed) and `sheets.js`'s `openSectorSheet()` already use, extracted into `sectorExposure()`; opacity-over-one-hue technique matches `design/target/insights-v2.html` exactly rather than a lightness-stepped ramp. Goal line is a real look-through fact, not an assumed one: finds the top sector's two largest dollar-contributing holdings from `SECTOR_WEIGHTS`, then cross-checks `js/explore.js`'s existing `lookExposure()` (already computes every underlying mega-cap's `via` fund list) for whether those two holdings actually share underlying companies before writing the "own the same companies" sentence — falls back to a plainer sentence if they don't. **Portfolio P/E:** paired bar (your blended P/E, from the existing `portfolioPE()`, vs `FUND_META.VOO.pe` — this app's standing SPY/S&P-500 proxy everywhere else: `riskStats()`'s beta, `benchmarkXirr()`, `pathValue()` all already benchmark against VOO, so reusing it here instead of inventing a separate SPY figure is the consistent choice) plus holdings ranked beneath by their own published P/E (`FUND_META`, the same figures `openPESheet()` already lists, just sorted descending instead of filing order). No goal line — a valuation multiple has no honest goal-date framing, so none was written. `renderModGrid()` now carries only Volatility (`index.html`'s `#modGrid` wrapper dropped from `.grid-2` to a plain single-column div, since a lone tile in a 2-column grid left a dangling empty cell). **Checked against real demo data, not just the mockup's spread-out illustrative values (session 1's clustering lesson):** the actual demo portfolio's contribution history is one $7,402.68 lump in a single month with the other eleven at exactly $0 — renders as one full bar and eleven near-baseline slivers (still visible at the 0.45 opacity floor, honestly showing the lumpy real shape rather than hiding it), and the derived "$616.89/month" pace is the plain average of what's on screen, matching the spec's own methodology, not a smoothing choice made after the fact. Sector exposure has an exact tie (Healthcare and Consumer Disc. both 10%) — renders as two identical-width bars, unambiguous only because each row carries its own text label, not color. Portfolio P/E (21.6) landed clearly separated from VOO (26.0) in this data pull, not a near-tie, but the bar-pair code path is the same one session 1's XIRR bars already handle gracefully at small deltas (4px minimum width floor). Live-verified in a browser against the running demo portfolio (not assumed from the mockup). Full suite green (52/52), golden master unchanged; not pushed.
- **2026-08-14** · **v2.6.152**: Insights v2, session 3 of 4 — surfaced four of the seven remaining #moreList candidates: Tax lots, Crash test, Geographic mix ("Where your money lives"), Asset worth. Financial independence and the goal-date projection stay behind the tap on purpose — Home's goal card already carries that number, and a second copy on Insights is the duplicate-number bug class CLAUDE.md names three prior fights over. Next-moves coach also stays behind the tap — advice isn't an insight and doesn't want a chart; not moved to Home this session, left as a call for later. **Form:** Tax lots is one proportional split bar (short-term vs long-term share of unrealized gain), not two magnitude bars — the real demo split is 98%/2%, and a magnitude bar would render the short-term side under 2px wide; the split-bar form makes the skew itself the point instead of hiding it, and the names list says which holding still carries a short-term lot and exactly when it clears (tax lots "changes behaviour: it tells you what not to sell yet", per the session brief). Crash test is a bar per CRASH_SCENARIOS entry (same beta-scaled dropCost formula openCrashSheet already used, now visualized) pairing with Risk's own goal line one card up. Geographic mix reuses Sector's exact sequential single-hue-ramp form (`locItems()`, the same aggregation openLocSheet already lists). Asset worth breaks from Delta's "top four" pattern deliberately: this portfolio holds six positions and can always name all of them (DESIGN-TARGET.md's own differentiator), so every held symbol gets its own self-scaled 1Y sparkline row instead of the four-class rollup openWorthSheet's full Chart.js view still uses. No goal line on Asset worth — its natural number (total value) already lives elsewhere; omitted per the brief's own rule rather than inventing one. **Sections:** the tab now reads in five parts — Performance (return vs VOO, contributions, monthly returns), Risk (beta axis, drawdown, crash test), Income & tax (tax lots), Under the hood (sector, geography, asset worth, P/E, volatility), More — using the exact "Performance"/"Risk"/"Income & tax"/"Under the hood" `<h2>` taxonomy sessions 1-2 had already put DE translations for in `js/i18n.js` but never wired to markup; "Future" (FI/projection/coach) stays unwired on purpose, matching this session's "leave behind the tap" call. **Two real layout bugs found checking against the live demo data, not the mockup's spread values (session 1/2's own lesson, hit twice more):** (1) crash scenario names ("2008 financial crisis", 21 chars) collided with their own bar in a first version that put the label in a 132px left gutter sized for XIRR's short "You"/"Same buys in VOO" labels — fixed by moving the label above the bar and the value inside the bar's own right edge (`--on-primary` white, since the largest bar already runs ~95% of the track and leaves no outside gutter either). (2) Geographic mix's real smallest buckets (Cash, Canada & other — 1% each) rendered under 2px wide with a pure-percentage bar width; added a 3px `min-width` floor to the shared `.seqtrack i` rule (`css/components.css`) — a no-op for Sector, whose real buckets never dropped this low, visible for Geographic mix's genuinely tiny ones. A third bug, not layout: the tax-lots bar's HTML `viewBox` height was left at its old 34px value while the JS that draws into it was written for 46px, so the bottom value row painted outside the SVG's box and visually collided with the names list below it — caught by reading the live rendered markup, not just the screenshot; fixed by syncing `index.html`'s `viewBox`/height to 46. All-new sentences (`js/insights.js`) get real German translations this session, not left as a gap — 15 new `js/i18n.js` entries (crash scenario names, both new module sub-labels, both new goal-line templates, the tax "already all long-term" fallback) — session 1/2's own Risk/Drawdown/XIRR/Contributions/Sector goal lines are untranslated too and were already that way before this session; not fixed here, flagged as pre-existing and out of scope. **Scroll height:** reconstructed pre-session (moreList still one-line for these four) ≈2618px measured live via DOM (hide the four new mod cards + four new `<h2>`s, re-measure, add back four real `.disclose` row heights) → post-session 3654px, +40%. Reads as scannable: the five section labels break the scroll into named parts, and every promoted module is a distinct shape (curve, ramp, sparkline row, paired bar) rather than another line of text, which is what let Sector/Risk/Drawdown avoid feeling like a queue already in sessions 1-2 — "Under the hood" (5 items) is the densest section and the first candidate to trim if a later session needs to tighten further, not cut here. Full suite green (52/52 — golden master unchanged, i18n-coverage 0 leaks, a11y clean on Insights), live-verified in a browser against the running demo portfolio; not pushed.
- **2026-08-15** · **v2.6.153**: Insights v2, session 4 of 4 — the last open call from session 3, then ship. **The coach decision:** "Next moves" is advice, not an insight, and doesn't want a chart (per the brief, so none was built). Moved it to Home rather than leaving it in Insights' `#moreList`, because these are time-sensitive nudges (idle cash, a lot about to turn long-term, a contribution streak going cold) and Home is where the owner actually lands — a nudge that only shows up after he remembers to open Insights and tap "More" isn't really timely. `js/insights.js`'s `renderCoach()` took an optional `targetId` param (defaults to the existing `#coachGrid`); a new one-line `renderHomeCoach()` points it at Home's new `#homeCoachGrid` instead — same cards, same per-card tap-through to `openInfoSheet()`, same `coachItems()` (still caps at 4, so the full set fits on Home with no "see all"). The Insights sheet path (`openCoachSheet`) is left intact, not deleted, even though `#moreList` no longer links to it — `test/i18n-coverage.spec.js`'s `SHEET_OPENERS` still calls it directly by name, and there was no reason to break that for a function that costs nothing sitting unreferenced. New Home section sits between "Portfolio insights" and "Coming up" (`index.html`), title reuses the "Next moves" dictionary entry already in `js/i18n.js` (unchanged since session 2) — no new German strings needed. **One real CSS bug found wiring the second mount point:** `#coachGrid`'s 2-column grid layout turned out to be entirely on the ID selector (`#coachGrid { display:grid; grid-template-columns: repeat(2,1fr); gap: var(--s-3) }`, `css/components.css`) — the `grid-2` class both containers' markup carries does nothing at all, no rule exists for it. Caught by reading the actual CSS before assuming the shared class would carry the layout; fixed by widening the selector to `#coachGrid, #homeCoachGrid`, same pattern applied to the two smaller `#coachGrid .icard`/`.icard .it`/`.icard b` rules for consistency (the `.icard`+`.cmove` combo's own unscoped `.cmove` rule already covers the grid/gap/padding half of that, so this part was closer to redundant than broken, but harmless to widen alongside the real fix). No maths touched — `coachItems()` itself is untouched, only where its output mounts. **Then shipped:** full suite run (52/52 green, golden master unchanged, i18n-coverage 0 leaks) before committing; nothing had gone live since v2.6.149, so this push also carries the six-slot categorical-palette fix and all three Insights v2 sessions (v2.6.150-152) that were sitting in the working tree unpushed. No further design changes made — session 3's tab-length judgment (2618px → 3654px, +40%, called scannable) stands untouched pending the owner actually looking at it on his phone; that's the next real feedback loop, not a number to keep guessing at from here.
- **2026-08-15** · **v2.6.154**: Insights visual fix session — five findings from running the live app (Playwright, iPhone 13 profile, dark, offline seed data), fixed by computed style rather than by eye. **1. Root cause, embossed chart text:** `css/base.css`'s bare `svg { fill:none; stroke:currentColor; stroke-width:1.75 }` was scoped for the icon-sprite pattern but had no scope — fill/stroke are inherited SVG properties, so every `<text>`/`<rect>`/`<circle>` drawn by js/insights.js's hand-built chart SVGs inherited a 1.75px near-white outline (confirmed live: chart `<text>` computed `stroke: rgb(242,244,248)` at 1.75px). Every icon `<svg>` in this app carries `aria-hidden="true"`, every hand-built chart SVG is `role="img"` instead — the two never overlap (grepped every `<svg>` in index.html to confirm) — so the rule is now scoped to `svg[aria-hidden="true"]`. Verified after: chart `<text>`/`<rect>` compute `stroke: none`. Then swept all seven chart-drawing functions (Risk, Drawdown, Crash test, Return-vs-benchmark, Contributions, Tax lots, Portfolio P/E) and added an explicit `stroke="none"` to every `<text>`/`<rect>` rather than leaving them to rely on the (now-fixed) inheritance chain — defense in depth against this exact class of regression recurring. **2. Risk axis marker collision:** session 1 only ever decluttered label TEXT position, never the CIRCLE markers themselves — on real clustered index-fund betas (0.78-1.18 band), VOO and VTI's raw pixel positions landed close enough for their circles to visually overlap outright (measured: 7.6px apart, each radius 4 = need ≥11px). Separately, the S&P benchmark's "S&P 1.00" label was drawn at a fixed position outside the label-declutter system entirely, so a holding near beta≈1.00 collided with it directly (measured: identical x). Fixed by folding the benchmark label into the same items array (still no circle of its own — its dashed tick line stays at the true value, unmoved) and running a two-pass (forward then backward) minimum-gap resolution on marker x *before* the existing per-row label declutter, so labels now start from already-separated markers. Verified on live data: markers ≥14px apart (was 7.6px), every same-row label pair ≥24px apart including the benchmark. **3. Chrome clipping — one real gap found, one investigated and not a token bug:** the pinned glass balance bar (`js/app.js`'s `pin` — always shown on Insights/Markets/Following, tabs without their own hero) had a real, unconditional top-clearance gap: `--bottom-clear`/`--safe-bottom` reserve space for the fixed tab bar + FAB at the *bottom*, but nothing reserved the equivalent at the *top* for this bar — non-demo mode had zero clearance at all (only demo mode's own badge padding happened to roughly, coincidentally, cover it). New `--minibar-h` token (composed from the same primitives `.minibar`'s own CSS already uses, not a measured pixel guess) applied as `.wrap` padding-top whenever `body.mbfix` is set, mirroring `--bottom-clear`'s own pattern exactly. Verified: title cleared the mini bar by 46px after (was inside it — top:51 vs minibar bottom:41 — before). Investigated, not a token-application bug: the Contributions goal line landing partly behind the tab bar at natural scroll position. Checked `--bottom-clear` directly — `.wrap`'s computed `padding-bottom` is exactly 148px, matching the token's own arithmetic, and the actual LAST card on the page (`#moreList`'s final row) clears the tab bar with 190px to spare at max scroll. The real cause, measured: Health (162px) + the "Performance" label + XIRR (204.5px) alone consume 366px of the 605px available before the fixed tab bar starts on this viewport — Contributions simply doesn't fit above the fold, which is normal for a page this tall and isn't what `--bottom-clear` was ever built to solve (it guarantees the page CAN be scrolled fully clear, not that every card starts already clear). Added `scroll-margin-bottom: var(--bottom-clear)` to `.mod` as real, low-risk token hygiene for any current or future explicit scroll-to-card call — but did not reorder or shrink Health/XIRR to force Contributions above the fold, since that's a content/design call (which card matters more on first paint), not a chrome-respecting bug fix, and out of this session's scope to decide unilaterally. **4. Health card vs. concentration:** `healthScore()`'s Diversification metric deliberately scores single-STOCK concentration only ("broad index funds ARE diversification; only single companies count as concentration" — its own pre-existing comment) — it was never designed to weigh sector or country weight, so it can't be the thing that's wrong: `healthScore()`'s `score` is a golden-master figure and moving it was off the table this session anyway. Confirmed the 100/100 is arithmetically correct for what it measures (queried `healthScore()` live: all four metrics legitimately 98-100). The overclaim was the CARD'S SENTENCE — "nothing is holding the grade down" reads as comprehensive on a screen that also shows 26% one sector / 83% one country. Rewrote it to name what the score does cover (single-stock risk, global mix, cost, cash) and say plainly that sector/country concentration isn't part of it, pointing at the cards that actually answer that (Sector exposure, Where your money lives) — a narrower, truthful claim, not softer wording for the same one. **5. Sequential ramp not reading as a ramp:** measured live — Sector's opacity formula (`0.25+0.75*v/max`) produced 0.53/0.53/0.52/0.45 across four real rows (an 0.08-wide band, imperceptible in a 9px bar); Geographic mix was worse, five of six rows within a 0.05 band. Both are the same shape of problem: one dominant row compresses everything else toward the opacity floor, and this app's own real portfolios are concentrated (that's finding #4), so this isn't an edge case. Dropped the opacity channel on both — length was already encoding the same magnitude, precisely and comparably; the color channel was double-encoding a job length alone already did, not doing a second job. One flat brand fill; length is the encoding, stated as such in both functions' comments. Full suite green (52/52), golden master unchanged (verified — no scoring formula touched), i18n-coverage 0 leaks (health card's new sentence has a matching `js/i18n.js` entry), a11y clean; not pushed.
- **2026-08-15** · **v2.6.155**: Surface the projection — the app's forgotten best feature. **Task 1, established before touching anything:** Phase 6's real engine (`runMonteCarloProjection()`, `js/monte-carlo.js` — 10,000-path fan chart, contribution-aware) is used in exactly one place today, Home's goal card (`renderGoal()`, `js/app.js`), and it DOES include a contribution there — `deriveMonthlyContribution()` (average $/month over real non-dividend lot history) feeds it by default. Insights' own "Where this is headed" (`renderProjection()`/`renderMonteCarlo()`) is a separate, older, weaker implementation — flat 4/7/10% deterministic compounding plus a bolted-on 400-path historical bootstrap, no percentile bands, already zero-contribution by design ("owner request: I want to see what my money can turn into"). Two different things, only one of them the real engine. **Task 2:** built the new module (below) on the real engine with `monthlyContribution:0` as the permanent main/headline run, plus a debounced (450ms) what-if input that re-runs the same engine with a typed monthly amount through the existing Worker path (`runProbabilisticGoal()`, unchanged) — never on the main thread. Before/after, live demo data, same engine: with the derived $2,916.97/mo contribution the demo's purchase history implies, a $200k-by-2036 goal already hits ~100% probability; at $0/mo it's 87.36%. The deployed module's own real numbers (goal $250,000, no target year set → 10y default): 66% with no more deposits, 89% after typing $500/mo in the what-if — the size of the swing is real, not cosmetic, exactly as expected from dropping a multi-thousand-dollar/month assumption to zero. **Task 3:** surfaced as `#projModCard`, a new "Future" section (the taxonomy label sessions 1-2 had already translated but never wired to markup) — fan chart (Lightweight Charts, same `drawGoalFan()` Home's card already used, generalized to take a second mount point via `el._lwcChart` instead of one module-level chart variable) visible with no tap, big number (probability if a goal is set, median value if not), goal-line sentence, what-if input. The OLD `openProjSheet()` stays reachable by tap on the card (outside the what-if input specifically — `e.target.closest('.whatif')` guards the click-through so typing a number doesn't also pop the old sheet) for its adjustable 5/10/20y deterministic view; its `#moreList` row was removed now that the same sheet has a second, better front door. **Task 4:** the memoization key/cache (`projModCalcKey`/`projModCalcResult`, mirroring `renderGoal()`'s own `goalCalcKey` pattern) are plain module-level variables — reset to null on every page load, so a fresh open always computes for real. It is not stale. The actual gap was that nothing showed when a result was last (re)computed; added `#projModAsOf`, mirroring `setStatus()`'s own age-bucketing convention (just now / Xs ago / X min ago / full date), tracking the timestamp of the last real (non-cached) calculation. **Task 5:** removed Insights' own ETF look-through entirely (`renderLook()`, `lookFetching`, `ensureLookQuotes()`, `openLookSheet()`, its `#moreList` row) — the Following tab's version (session 3) carries more (live quotes, day % change) and was already the better of the two. Checked for other links: `test/i18n-coverage.spec.js`'s `SHEET_OPENERS` called `openLookSheet()` directly — removed that entry too; nothing else referenced any of the deleted names. **Unplanned but required to ship green:** adding "years"/"/month" as new dictionary candidates exposed three PRE-EXISTING, never-caught i18n gaps the ratchet had no way to see before (no prior session had ever added those exact words as candidates) — Crash test's recovery-time sentence, the old Projection sheet's "In N years:" line, and session 1's XIRR/Contributions goal lines. Fixed all three (one required renaming a local variable that shadowed the global `t()` translator) rather than ship a red suite or dodge the dictionary entries my own new module genuinely needed. Full suite green (52/52), golden master unchanged (no scoring/engine formula touched, verified), i18n-coverage 0 leaks, a11y clean; not pushed.
- **2026-08-15** · **v2.6.156**: Ship, verify end-to-end, fix. **Shipped first (v2.6.155):** the projection-surfacing work from the previous session, unpushed in the working tree, went out in three commits with the full suite green. **Then walked every surface** in Playwright at the iPhone 13 profile with external hosts route-aborted (seed data only), verifying by DOM and computed style rather than screenshot: five tabs, eighteen sheet openers, dark and light, EN and DE, USD and EUR, privacy on and off, plus an emptied portfolio. **The named lead, confirmed and fixed:** Home's goal card ran `runMonteCarloProjection()` with a contribution rate derived from lot history (`deriveMonthlyContribution()`) while the new Insights module ran the same engine at zero — one goal, 100% on Home and 87% on Insights, measured on the demo dataset. Same defect class as the Home-vs-Portfolio total, the three theme-colour copies and the three hardcoded active states. Reconciled per the owner's call — the MAIN projection excludes contributions — into one source of truth in `js/app.js`: `PROJECTION_MODEL` (the frozen constants), `projectionParams()` (which pins `monthlyContribution` to 0 for every headline run), `projectionKey()`, `runProjection()` and `projectionComputedAt()`. Both surfaces now call it, and both read one shared params-keyed cache, so with the engine's fixed seed a matching key is a *guarantee* of a matching number, not just a speed win. `deriveMonthlyContribution()` deleted — the Insights what-if input is now the only place in the app a contribution figure enters a projection. New `test/projection-consistency.spec.js` (4 tests) holds all of it. **Cache is a Map, not one slot:** `renderAll()` re-renders both surfaces on every price poll, and the headline run and the what-if run are live at the same time — a single slot would have missed on both every tick and fired two 10,000-path runs per poll. Capped at 8, oldest evicted. **Three more real findings, each reproduced before being fixed:** (1) `drawGoalFan()` tore its Lightweight Charts instance down and rebuilt it on every `renderAll()` — one empty frame per price poll, on both mount points, for a chart whose data had not changed; now guarded by a draw key over result/goal/theme/privacy/currency (verified: `recreated: false` across a tick that previously read `true`). (2) The Contributions goal line hardcoded `$100` — it read as dollars in euro mode next to a converted `€530.53`, and stayed unmasked while every other figure in the same sentence was hidden by the privacy toggle; the bump is now 100 units of the *displayed* currency converted back to the USD the maths runs in (`100/rate()`), rendered through `fmt()`. (3) Four section headings rendered English under German ones — "Stay on top", "Portfolio insights", "Coming up" on Home, "More" on Insights — plus the month unit at four `js/insights.js` call sites ("33 months early" inside an otherwise fully translated sentence). Keyed with the count (`"%1 months"`, not a bare `"months"`) because `test/i18n-coverage.spec.js` matches candidates by plain substring and a bare `"month"` would flag every "monthly" in any not-yet-translated sentence. **One candidate finding dropped by verification, not shipped:** the Portfolio group headers read `"ETFs · 5$138,740.64"` in `textContent`, which looks like a missing separator — measured the boxes instead of trusting the string, and the `<em>` sits 278px right of the label via `margin-left:auto` in a flex `.section__label`. Not a bug. **Two Backlog entries retired as already-fixed** after measuring rather than re-reading the note: `TAB_ORDER` is the five-tab order (`js/app.js:77`), and the Settings sheet scrolls (`#editSheetBody` computes `overflow-y: auto`, 1297px of content in a 519px box). Full suite green (56/56 — golden master unchanged, no scoring or engine formula touched; i18n-coverage 0 leaks; a11y clean).
- **2026-08-16** · **v2.6.158**: Four follow-ups from v2.6.157's own diff, worked in order. **1. `#projModBig` now genuinely never moves for the what-if input** — it did, despite the v2.6.157 entry's intent, because `apply()` painted from `result` (the what-if run) whenever `contrib>0`; `test/projection-consistency.spec.js:62` asserted `after > before` and was passing on the bug. The owner's actual requirement (the reason this card exists) is that the primary output is growth with NO deposits and the typed figure is a secondary what-if — a headline that moves leaves no stable "no more deposits" answer visible anywhere. `renderProjMod()` (`js/insights.js`) now always resolves `baseResult` (params pinned to `monthlyContribution:0`) for `#projModBig`, its sub-caption ("no more deposits", no longer switching to "$X/mo added") and `#projModGoalLine` — none of the three read the what-if run any more. The what-if's only two outputs are `#projWhatIfResult` (the existing delta sentence) and, new, a 1.5px dashed `--brand` median line drawn over the base cone (`drawGoalFan()`'s new `whatIf` param, `js/app.js`) with an inline `+$250/mo`-style label, appearing only when the input is >0 and removed when cleared. `p50`'s `autoscaleInfoProvider` now folds in the what-if median's own max so it can't draw off the top of the pane — verified at $2,000/mo: chart's own `priceScale('right').getVisibleRange().to` came back exactly `581975.03`, matching the what-if result line's own reached figure, with the scale's built-in 14% top margin still giving it headroom; series count went 2→3 (p50, goal line, what-if line) confirming the overlay actually drew. `test/projection-consistency.spec.js`'s third test rewritten (comment records the flip and why, this session) to assert `#projModBig` is unchanged after typing 1500 while `#projWhatIfResult` becomes visible and a fourth chart series appears, then that clearing the input removes both. **2. The band-fill discrepancy in the v2.6.157 entry doesn't reproduce.** `getComputedStyle` for `--brand-soft` and `--primary-tint` both print `rgba(124, 108, 255, 0.14)` — identical, exactly DESIGN-TARGET.md's value — and the fan primitive's `fill` argument is that same string verbatim (`js/app.js`: `soft=cvar('--brand-soft')`, passed straight through). A fresh 60%-width canvas pixel read, sampled away from antialiased boundary rows this time, gives a clean single-layer band at `rgba(128,106,255,0.141)` and a clean double-layer (p25–p75 overlap) band at `rgba(126,107,255,0.263)` — both within Canvas2D's inherent 8-bit premultiplied-alpha rounding of the exact math (single 0.14, double `1-(1-0.14)²=0.2604`). The v2.6.157 entry's own `rgba(125,101,255,0.169)` reading — alpha between the single- and double-layer values — was very likely a boundary/antialiased pixel, not a distinct third value; no token or fill-string bug exists to fix. No code changed for this item. **3. New `test/active-state.spec.js` coverage for the three duplicated Home/Portfolio appbar controls** (priv/theme/ccy), per CLAUDE.md's trap list (a hardcoded-and-never-cleared active state has shipped three times): a static-markup check that neither appbar's `data-act` buttons hardcode `aria-pressed` (6 matches, one assertion each), a load-state check that both instances' `aria-pressed`/`aria-label`/`<use href>` match the real `state.view`/`dataset.theme` rather than the markup's baked-in default icons, a two-direction sync test (toggle from Home → assert Portfolio matches; toggle from Portfolio → assert Home matches, for all three controls), and a regression guard for the icon-markup bug v2.6.157 fixed — all six button instances resolve to exactly one `<svg aria-hidden="true">` computing `fill:none` with a non-`none` stroke. Suite count: 56 → 60 (4 new tests, one of which loops all three controls). **4. `font-variant-numeric: tabular-nums lining` is invalid CSS** (`lining` isn't a keyword this property accepts — `lining-nums` is) — confirmed by isolated probe (`font-variant-numeric: normal` ancestor to kill inheritance, then the exact broken string on a child): computed value came back `normal`, i.e. the whole declaration was silently dropped, not partially applied. In practice the damage was narrower than it looked: `css/base.css`'s own `*` rule declares valid `tabular-nums;` immediately before its own broken `lining` upgrade attempt, and since `font-variant-numeric` inherits, most numbers were quietly saved by that inherited fallback rather than actually jittering — but every other broken declaration (`.hval`/`.hpl`/etc. group, `.mv-pct`, `.section__label em`, `.heatmap`) was a dead no-op relying on that same lucky inheritance, not a real, independent guarantee. Fixed at all 5 broken call sites (`css/base.css:47,130`; `css/components.css:1359,2285,2581` — `components.css:314` and `:326` were already correct, added in v2.6.157's own `.whatif` work) plus the source, `DESIGN-TARGET.md`'s type-scale section, which is where the bad string came from originally (`design/target/five-tabs.html`/`insights-v2.html` carry the same bug in their own inline `<style>` and were flagged, not touched — out of this session's scope). Verified post-fix, live, cache-busted to rule out stale CSS: `#tvNum` (Home), `.mv-pct` (Home), `.section__label em` (Portfolio), and `.heatmap` (Insights) all now compute `lining-nums tabular-nums`. Full suite green (60/60), golden master unchanged (verified — no scoring/engine formula touched), not pushed. **One item noticed, not fixed, out of scope:** `design/target/five-tabs.html` and `design/target/insights-v2.html` both still carry the same invalid `tabular-nums lining` in their own inline `<style>` blocks — they're static reference mockups, not app code, but will keep reproducing the same bug in any future session that copies from them literally.
- **2026-08-16** · **v2.6.157**: Three visual defects the owner reported from the live app (iPhone 13, dark, demo data), fixed at cause. **1. The Monte Carlo chart was not a fan.** `drawGoalFan()` drew p10 and p90 as two thin dashed grey `LineSeries` with nothing between them, so `#projModCard` read as three unrelated guide lines rather than one distribution, and the goal line used `--warn` (amber — not a palette token, and colour that DESIGN-TARGET.md reserves for status). Lightweight Charts has no band series and an `AreaSeries` fills to the BOTTOM of the pane rather than to a second series, so a cone cannot be built from the stock series types without an opaque mask over the lower half — which would then have made the inner-quartile overlay impossible. Rebuilt as two series primitives on the p50 series: `fanBandPrimitive()` paints p10–p90 and p25–p75 as filled polygons in `--brand-soft` at zOrder `bottom` (the same fill twice, so the middle half reads roughly twice as dense; no stroke on either band edge), and `fanLabelPrimitive()` draws the end-of-range p10/p90 values in 11px `--text-faint` plus the goal's own `Goal $250K` in 11px/650 `--text-mut`, in media coordinate space so the type is 11 CSS px at any DPR, with a one-line-height collision test in priority order goal → p90 → p10 (which is what makes "drop p10, not both" fall out without a special case). p50 is now a plain 2px `--brand` `LineSeries` — the only line — and the goal line is 1px dashed `--text-mut`. Since p10/p90 are no longer series, the price scale can no longer see them: p50 carries an `autoscaleInfoProvider` covering the whole cone plus the goal, or the band clips at the top and the goal line falls off the chart. `js/monte-carlo.js` grew `p25`/`p75` (additive — every existing p10/p50/p90 figure is byte-identical, and `runMonteCarloProjection` is still the same pure function); `--brand-soft` added to `css/tokens.css` as an alias of the pre-existing `--primary-tint` rather than a duplicate value, so DESIGN-TARGET.md's own token name resolves and no literal rgba() enters `js/app.js`. **Verified by canvas pixel read, not by screenshot** — column at 60% of chart width: p50 `rgba(124,107,255,0.929)`, midway p50↔p90 `rgba(125,101,255,0.169)`, background `rgba(0,0,0,0.000)`. Band width at t=0 is 0 translucent rows against 62 at 60% width, confirming the cone starts at a single point (`fan.p10[0] == fan.p25[0] == fan.p50[0] == fan.p75[0] == fan.p90[0] == v0`; year 0 is unsimulated). **2. The what-if input read as disabled.** `#projWhatIfInput` had always existed, but as an inline `Add [ 0 ] /month` — an 84px box with a dim right-aligned `0`, no label above it — which the owner read as placeholder chrome and never tapped ("there is nowhere to enter contributions"). An affordance failure, not a missing feature: same input, same debounced handler. Now a `.section__label` above it, a full-width 44px field on `--surface` with a 1px `--line` border at radius 10, a `$` prefix in `--text-mut`, the value in `--text` at the 16px iOS-zoom floor `css/base.css` already enforces, a real `--text-faint` placeholder, and `:focus-visible` moving the border to `--brand` on top of the standard ring. Three `--brand-soft` preset chips (`$100`/`$250`/`$500`) write into the same input and go through the same re-run path — they are what makes the control discoverable at a glance. Beneath it, one 12.5px `--text-mut` line naming the delta against the no-deposit run ("Adding $250.00/month reaches $312,127.79 — $36,738.72 more than with no deposits."), absent rather than zeroed when the input is empty. `renderProjMod()` now builds two param sets: `base` at `monthlyContribution:0` (identical to the one Home's `renderGoal()` hardcodes, so both surfaces stay on one shared cache key) and the what-if's own derived run; when the input is empty the two collapse to one cache hit. Confirmed at 0: `projectionParams(v0, 10, 250000, 0).monthlyContribution === 0`, key `250000|10|148050|0.00`, result line hidden. **3. The header controls.** (a) `#privBtn`/`#themeBtn` computed `fill: rgb(0,0,0)`, `stroke: none` — the UA defaults — and `#ccyBtn` had no `<svg>` at all. Root cause was not the recent `svg[aria-hidden="true"]` scoping in `css/base.css:170` (that fix is correct and stands): all three buttons destroyed the markup the rule keys on at runtime. `paintPriv()` and the theme handler overwrote `innerHTML` with their own hand-rolled `<svg>` strings carrying no `aria-hidden`, and `js/portfolio.js`'s `renderHeader()` replaced `#ccyBtn`'s contents with a `$`/`€` text glyph. Nothing writes icon markup any more: each button keeps the one sprite `<svg aria-hidden="true">` `index.html` ships and only its `<use href>` changes, so the attribute cannot be written away again. `eyeOff` (relocated from `js/app.js`, same drawing) and `euro` (Lucide, matching `dollar`) added to the sprite. Before → after, all three: `fill rgb(0,0,0) / stroke none / aria-hidden null` (and no svg at all for ccy) → `fill none / stroke rgb(144,153,172) / stroke-width 1.75px / aria-hidden true / 24x24`. (b) All three change state globally but lived only in the Portfolio appbar, so they were unreachable from Home — the tab the app lands on, and which shows the total, today's delta and Price highlights. The same group now sits in Home's appbar, addressed by `data-act` rather than by id, with one painter (`paintAct()`) and one delegated click listener for both instances, so a second copy cannot drift from the first. After a single toggle from Home, both instances read identically: priv `Show amounts`/`true`/`#i-eye-off`, theme `Toggle theme`/`false`/`#i-sun`, ccy `Switch to dollars`/`#i-euro`. Three new `js/i18n.js` aria-label entries plus the what-if label and result sentence. Full suite green (56/56), golden master unchanged (verified, not assumed — no scoring or engine formula touched), i18n-coverage 0 leaks, a11y clean; not pushed.
- **2026-08-17** · **v2.6.159**: Two follow-ups. **1. The what-if fan chart's autoscale had no headroom.** `drawGoalFan()`'s p50 `autoscaleInfoProvider` (`js/app.js`) returned a `maxValue` exactly equal to the highest drawn value (the p90 band top, the goal, or the what-if median — whichever was largest), relying entirely on the chart's `scaleMargins.top` (a fixed 14%-of-pane-height fraction) for breathing room above it. That margin is real screen space, but it's spent getting from the container edge down to the highest point itself — none of it is left for that point's own stroke or the `+$X/mo` label drawn above it (confirmed by reproducing the reported case live: `getVisibleRange().to` came back bit-for-bit equal to the what-if median's own reached value, and changing `scaleMargins.top` after the fact moved the data point's pixel position, proving the margin was already fully claimed). Fixed by pushing `maxValue` itself above the raw max by a price-unit amount solved from a real pixel budget — half the thickest line's stroke (p50's 2px) + the label's own glyph height (measured via `canvas.measureText`, not guessed) + a 4px buffer — converted through the pane's actual geometry (`chart.timeScale().height()`, measured immediately after `createChart`, plus the fixed 180px `.chart-box--sheet` height and the existing margin fractions), solving the self-referential equation exactly rather than approximating (expanding `maxValue` also expands the span mapped into the same margin-adjusted pixel height, so a naive addition would partly cancel itself). Verified at $2,000/mo and $100/mo: gap between `getVisibleRange().to` and the what-if median's own maximum is positive in both cases (16.1px / 66.6px, was 0px / 58.2px), and a canvas pixel scan of the chart's top rows shows no drawn pixels above row 15 at either setting. Checked the `+$X/mo` label against the p90 end label for collision at $2,000/mo per the existing goal→p90→p10 priority chain: bounding boxes measured 19.6–30.8px (what-if) and 62.8–73.1px (p90) — no overlap, both render. **2. `design/target/five-tabs.html` and `design/target/insights-v2.html`** still carried the invalid `font-variant-numeric: tabular-nums lining` in their own inline `<style>` blocks (v2.6.158 fixed every app file but flagged these two reference mockups as out of scope) — fixed to `lining-nums` at both sites; computed `font-variant-numeric` on `.n` now reads `lining-nums tabular-nums` in both files. No other lines touched in either mockup. Full suite green, golden master unchanged, not pushed until this entry.
- **2026-08-17** · **v2.6.160**: session 6, owner-specified scope change — the projection card was framed around the goal (a percentage-chance headline), which the owner kept misreading as something else; what he actually wants there is "what will this be worth". Two changes. **(1) `#projModCard` (Insights) is a value projection now, goal-free.** New `#projHorizonSeg` (5Y·10Y·20Y·30Y, default 10, `js/insights.js`'s `projModHorizon`) picks the headline's horizon; no hardcoded `aria-selected` in `index.html`'s markup, state painted at render time by `renderProjMod()` (same `syncSel` convention `#metricSeg`/`#rangeSeg`/`#moverToggle` already use — covered in `test/active-state.spec.js`, including a real-effect check that the sub-caption's year count tracks the clicked button, not just the lit-count invariant). `#projModBig` is now the median value (`fmt()`, currency) at the selected horizon, not `probabilityOfGoal*100`; new `#projModRange` beneath it states the p10-p90 band in the card's muted secondary style — a lone median with no spread was the false-precision problem this whole rework exists to fix. Every goal reference is gone: `#projModGoalLine` removed from the DOM, `renderProjMod()`'s params always carry `goal:null`. **Re-slice, not re-run**: `monte-carlo.js` already computes every year's percentiles from the same simulated paths, so switching horizon reads a different index (`sliceFan()`) out of one `PROJ_MOD_YEARS=30` run instead of re-simulating 10,000 paths per click — confirmed live: median/p10/p90 increase monotonically across 5/10/20/30y, and `test/projection-consistency.spec.js`'s new horizon test asserts `projCache.size` doesn't grow when the horizon changes. **(2) Home's goal card (`#goalBody`, `js/app.js` `renderGoal()`) is enriched, not replaced.** It already showed current-vs-target with the progress bar and the percentage chance by target year (`#goalPct`) — both untouched, verified unchanged against a direct `runProjection()` call for the same inputs (printed, not just asserted: `test/projection-consistency.spec.js`'s first test logs both figures). New: the projected date the MEDIAN path itself reaches the goal, from a second always-30-year/no-goal/no-contribution run (`capParams`) whose params happen to match Insights' own base run, so the two can share a cache hit when the portfolio value matches. Capped and honest — searches only years 0-30 and states plainly ("The median path doesn't reach your goal within 30 years.") rather than extrapolating past the cap, per the owner's explicit instruction. Two new DE dictionary entries for this line; `"Likely range %1"` replaces two now-dead `#projModGoalLine` translation strings (removed alongside two pre-existing, already-unreferenced dead entries from the same block, found while editing it — not otherwise investigated this session). `test/projection-consistency.spec.js` rewritten in full: the old cross-surface "same probability on both cards" tests no longer apply (Insights' card no longer shows a probability at all) — replaced with Home-vs-direct-engine verification, the goal-line removal, the horizon re-slice/cache guarantee, and a `money()`-based version of the existing "`#projModBig` pinned to zero-contribution" pinning test. **Golden master unaffected and not regenerated** — `test/golden/capture.js`'s `collectFigures()` captures `totals`/`xirr`/`monthlyDietz`/`risk`/`health`/`annualIncome`/holdings only; no goal or projection figure was ever in it, and no pure financial-math function (`js/core.js`, `js/insights.js`'s `riskStats()`/`healthScore()`) was touched this session. No other files touched.
- **2026-08-17** · **v2.6.161**: session 7, five follow-ups from v2.6.160. **(1) Growth-rate decay — diagnosed, NOT a bug.** The prior session's printed median growth rates (5Y 7.23% → 30Y 6.50%) looked like a decaying-then-converging artifact; reproduced exactly by dividing the CORRECT run's medians by a STALE portfolio total ($148,041.36, a different browser-reload's randomized demo snapshot) instead of the $153,708.33 that run actually used as `v0`. Recomputed against the correct `v0`, the annualized rate is flat at 6.36-6.44% across all four horizons (confirmed again at 200,000 paths), matching the model's Jensen's-inequality-predicted geometric rate of `0.07 - 0.12²/(2·1.07²) ≈ 6.37%` almost exactly — the model draws i.i.d. NORMAL annual real returns and compounds multiplicatively, so the median genuinely runs below the 7% arithmetic mean; this is correct, expected behaviour for that model, not lognormal-flat. `fan.p50.length`/off-by-one/year-0-as-a-step/sliceFan were all checked directly and ruled out. No code changed. **(2) `t` shadowing fixed at the source.** `js/app.js` `renderGoal()`'s `const t=totals('all')` renamed to `tot` (the convention `js/insights.js` `openCrashSheet()` already used for the identical reason); the `window.t` workaround from the prior session is gone, replaced with plain `t` calls. Grepped every other `const/let t=totals(...)` shadow in `js/app.js`/`js/insights.js`/`js/portfolio.js`/`js/sheets.js`/`js/explore.js` (17 found) — none of the rest ever calls `` t`` `` inside its own scope, so none is currently broken; not touched. **(3) Home's "chance by ⟨year⟩" wrapped.** Now `` t`chance by ${year}` `` with new EN/DE dictionary entries; visual output unchanged (same inline style, same text). `test/i18n-coverage.spec.js`'s `englishCandidates()` builds its candidate list only from existing `window.i18nDE` keys and `index.html`'s static text — it never parses `.js` source — so a brand-new hardcoded string in a template literal is invisible to it until it already has a dictionary entry; that's exactly how this string shipped several "0 leaks" sessions untranslated. Documented, not fixed: one line added to `UPGRADE_PLAN.md`'s Backlog. **(4) Golden master now covers the projection card.** `test/helpers.js` `collectFigures()` gained `projection` (median/p10/p90 at 5/10/20/30y, direct `runMonteCarloProjection()` call, same fixed seed) and `goalProbability` (fixed `{amt:200000,year:2036}` fixture, independent of `js/demo.js`'s own goal which has no year). Regenerated once; diffed against the pre-regeneration file — the two new keys are the ONLY change, every pre-existing key (`totals`, `xirr`, `monthlyDietz`, `risk`, `health`, `annualIncome`, `holdings`, `holdingsCount`, `totalValText`) is byte-identical. **(5) Cross-surface check hardened.** `test/projection-consistency.spec.js`'s existing "Home vs. direct engine call" test (added session 6, already a real `expect()`, not just a `console.log`) is joined by a new test pinning the same rendered figure against the golden master's frozen `goalProbability` — catches a drift in the engine/model constants themselves, which the live-vs-live comparison can't (both sides call the same code). File header rewritten to record what each of the file's 5 tests protects and why, since this file has now been rewritten or extended in 3 straight sessions. No math files touched; full suite green.
